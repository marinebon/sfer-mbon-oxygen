[
  {
    "objectID": "cruise_anoxic_depth/listing.html",
    "href": "cruise_anoxic_depth/listing.html",
    "title": "cruise_anoxic_depth",
    "section": "",
    "text": "Click on any element for more details.\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nSV18067 Report\n\n\n\n\n\n\n\n\n\n\n\n\nSV18173 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWB22215 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0603 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0612 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0618 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0623 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0704 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0718 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0802 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0807 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0901 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0906 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0914 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0919 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0923 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1004 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1007 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1015 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1018 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1102 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1106 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1113 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1116 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1202 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1207 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1212 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS14335 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS15103 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS15148 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS15208 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS15264 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS15320 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS16004 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS16074 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS16207 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS16263 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS16319 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS17030 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS17086 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS17170 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS17212 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS17282 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS18008 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS18120 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS18218 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS18285 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS18351 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS19028 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS19119 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS19210 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS19266 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS19322 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS20006 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS20231 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS20279 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS20342 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS21032 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS21093 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS21151 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS21212 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS21278 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS21338 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS22022 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS22072 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS22141 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS22281 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS22337 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS23011 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS23061 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS23203 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS24139 Report\n\n\n\n\n\nNo matching items"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS23203.html",
    "href": "cruise_anoxic_depth/batched_reports/WS23203.html",
    "title": "WS23203 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS23203.html#cruise-ws23203",
    "href": "cruise_anoxic_depth/batched_reports/WS23203.html#cruise-ws23203",
    "title": "WS23203 Report",
    "section": "Cruise WS23203",
    "text": "Cruise WS23203\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    84             6994             23696            1.3486\n2    84             6994             23696            5.9850"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS23203.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS23203.html#anoxic-depth-map",
    "title": "WS23203 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS23011.html",
    "href": "cruise_anoxic_depth/batched_reports/WS23011.html",
    "title": "WS23011 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS23011.html#cruise-ws23011",
    "href": "cruise_anoxic_depth/batched_reports/WS23011.html#cruise-ws23011",
    "title": "WS23011 Report",
    "section": "Cruise WS23011",
    "text": "Cruise WS23011\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    73             4505             25994            3.1448\n2    73             4505             25994            5.6046"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS23011.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS23011.html#anoxic-depth-map",
    "title": "WS23011 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS22281.html",
    "href": "cruise_anoxic_depth/batched_reports/WS22281.html",
    "title": "WS22281 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS22281.html#cruise-ws22281",
    "href": "cruise_anoxic_depth/batched_reports/WS22281.html#cruise-ws22281",
    "title": "WS22281 Report",
    "section": "Cruise WS22281",
    "text": "Cruise WS22281\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    73             5431             24969            2.7499\n2    73             5431             24969            4.8476"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS22281.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS22281.html#anoxic-depth-map",
    "title": "WS22281 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS22072.html",
    "href": "cruise_anoxic_depth/batched_reports/WS22072.html",
    "title": "WS22072 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS22072.html#cruise-ws22072",
    "href": "cruise_anoxic_depth/batched_reports/WS22072.html#cruise-ws22072",
    "title": "WS22072 Report",
    "section": "Cruise WS22072",
    "text": "Cruise WS22072\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    76             6530             23007            2.6545\n2    76             6530             23007            5.1897"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS22072.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS22072.html#anoxic-depth-map",
    "title": "WS22072 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS21338.html",
    "href": "cruise_anoxic_depth/batched_reports/WS21338.html",
    "title": "WS21338 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS21338.html#cruise-ws21338",
    "href": "cruise_anoxic_depth/batched_reports/WS21338.html#cruise-ws21338",
    "title": "WS21338 Report",
    "section": "Cruise WS21338",
    "text": "Cruise WS21338\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    71             5691             25855            2.4974\n2    71             5691             25855            5.5520"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS21338.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS21338.html#anoxic-depth-map",
    "title": "WS21338 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS21212.html",
    "href": "cruise_anoxic_depth/batched_reports/WS21212.html",
    "title": "WS21212 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS21212.html#cruise-ws21212",
    "href": "cruise_anoxic_depth/batched_reports/WS21212.html#cruise-ws21212",
    "title": "WS21212 Report",
    "section": "Cruise WS21212",
    "text": "Cruise WS21212\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    70             4284             29480            1.4935\n2    70             4284             29480            4.5662"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS21212.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS21212.html#anoxic-depth-map",
    "title": "WS21212 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS21093.html",
    "href": "cruise_anoxic_depth/batched_reports/WS21093.html",
    "title": "WS21093 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS21093.html#cruise-ws21093",
    "href": "cruise_anoxic_depth/batched_reports/WS21093.html#cruise-ws21093",
    "title": "WS21093 Report",
    "section": "Cruise WS21093",
    "text": "Cruise WS21093\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    64             4050             53252         -104.1946\n2    64             4050             53252            5.2020"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS21093.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS21093.html#anoxic-depth-map",
    "title": "WS21093 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS20342.html",
    "href": "cruise_anoxic_depth/batched_reports/WS20342.html",
    "title": "WS20342 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS20342.html#cruise-ws20342",
    "href": "cruise_anoxic_depth/batched_reports/WS20342.html#cruise-ws20342",
    "title": "WS20342 Report",
    "section": "Cruise WS20342",
    "text": "Cruise WS20342\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    67             4503             24573            2.7412\n2    67             4503             24573            5.1600"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS20342.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS20342.html#anoxic-depth-map",
    "title": "WS20342 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS20231.html",
    "href": "cruise_anoxic_depth/batched_reports/WS20231.html",
    "title": "WS20231 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS20231.html#cruise-ws20231",
    "href": "cruise_anoxic_depth/batched_reports/WS20231.html#cruise-ws20231",
    "title": "WS20231 Report",
    "section": "Cruise WS20231",
    "text": "Cruise WS20231\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    43             3054             30048           -1.0535\n2    43             3054             30048            4.6257"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS20231.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS20231.html#anoxic-depth-map",
    "title": "WS20231 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS19322.html",
    "href": "cruise_anoxic_depth/batched_reports/WS19322.html",
    "title": "WS19322 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS19322.html#cruise-ws19322",
    "href": "cruise_anoxic_depth/batched_reports/WS19322.html#cruise-ws19322",
    "title": "WS19322 Report",
    "section": "Cruise WS19322",
    "text": "Cruise WS19322\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    70             4557             24279            3.2941\n2    70             4557             24279            5.8361"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS19322.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS19322.html#anoxic-depth-map",
    "title": "WS19322 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS19210.html",
    "href": "cruise_anoxic_depth/batched_reports/WS19210.html",
    "title": "WS19210 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS19210.html#cruise-ws19210",
    "href": "cruise_anoxic_depth/batched_reports/WS19210.html#cruise-ws19210",
    "title": "WS19210 Report",
    "section": "Cruise WS19210",
    "text": "Cruise WS19210\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    42             2486             29440            2.5009\n2    42             2486             29440            5.1375"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS19210.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS19210.html#anoxic-depth-map",
    "title": "WS19210 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS19028.html",
    "href": "cruise_anoxic_depth/batched_reports/WS19028.html",
    "title": "WS19028 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS19028.html#cruise-ws19028",
    "href": "cruise_anoxic_depth/batched_reports/WS19028.html#cruise-ws19028",
    "title": "WS19028 Report",
    "section": "Cruise WS19028",
    "text": "Cruise WS19028\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    33             1345             62034            3.2756\n2    33             1345             62034            5.7658"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS19028.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS19028.html#anoxic-depth-map",
    "title": "WS19028 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS18285.html",
    "href": "cruise_anoxic_depth/batched_reports/WS18285.html",
    "title": "WS18285 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS18285.html#cruise-ws18285",
    "href": "cruise_anoxic_depth/batched_reports/WS18285.html#cruise-ws18285",
    "title": "WS18285 Report",
    "section": "Cruise WS18285",
    "text": "Cruise WS18285\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    64             4324             24152            1.1042\n2    64             4324             24152            8.5705"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS18285.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS18285.html#anoxic-depth-map",
    "title": "WS18285 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS18120.html",
    "href": "cruise_anoxic_depth/batched_reports/WS18120.html",
    "title": "WS18120 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS18120.html#cruise-ws18120",
    "href": "cruise_anoxic_depth/batched_reports/WS18120.html#cruise-ws18120",
    "title": "WS18120 Report",
    "section": "Cruise WS18120",
    "text": "Cruise WS18120\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    19              933             60286            2.6945\n2    19              933             60286            4.9503"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS18120.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS18120.html#anoxic-depth-map",
    "title": "WS18120 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS17282.html",
    "href": "cruise_anoxic_depth/batched_reports/WS17282.html",
    "title": "WS17282 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS17282.html#cruise-ws17282",
    "href": "cruise_anoxic_depth/batched_reports/WS17282.html#cruise-ws17282",
    "title": "WS17282 Report",
    "section": "Cruise WS17282",
    "text": "Cruise WS17282\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    63             4745             48832            2.7664\n2    63             4745             48832            5.0644"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS17282.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS17282.html#anoxic-depth-map",
    "title": "WS17282 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS17170.html",
    "href": "cruise_anoxic_depth/batched_reports/WS17170.html",
    "title": "WS17170 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS17170.html#cruise-ws17170",
    "href": "cruise_anoxic_depth/batched_reports/WS17170.html#cruise-ws17170",
    "title": "WS17170 Report",
    "section": "Cruise WS17170",
    "text": "Cruise WS17170\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    25              486             89436            2.8004\n2    25              486             89436            4.4252"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS17170.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS17170.html#anoxic-depth-map",
    "title": "WS17170 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS17030.html",
    "href": "cruise_anoxic_depth/batched_reports/WS17030.html",
    "title": "WS17030 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS17030.html#cruise-ws17030",
    "href": "cruise_anoxic_depth/batched_reports/WS17030.html#cruise-ws17030",
    "title": "WS17030 Report",
    "section": "Cruise WS17030",
    "text": "Cruise WS17030\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    30             1833             43764            2.8508\n2    30             1833             43764            5.6084"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS17030.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS17030.html#anoxic-depth-map",
    "title": "WS17030 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS16263.html",
    "href": "cruise_anoxic_depth/batched_reports/WS16263.html",
    "title": "WS16263 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS16263.html#cruise-ws16263",
    "href": "cruise_anoxic_depth/batched_reports/WS16263.html#cruise-ws16263",
    "title": "WS16263 Report",
    "section": "Cruise WS16263",
    "text": "Cruise WS16263\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    29             1610             42306            2.0717\n2    29             1610             42306            4.9545"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS16263.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS16263.html#anoxic-depth-map",
    "title": "WS16263 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS16074.html",
    "href": "cruise_anoxic_depth/batched_reports/WS16074.html",
    "title": "WS16074 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS16074.html#cruise-ws16074",
    "href": "cruise_anoxic_depth/batched_reports/WS16074.html#cruise-ws16074",
    "title": "WS16074 Report",
    "section": "Cruise WS16074",
    "text": "Cruise WS16074\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    29             1152             72889            2.5237\n2    29             1152             72889            5.5489"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS16074.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS16074.html#anoxic-depth-map",
    "title": "WS16074 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS15320.html",
    "href": "cruise_anoxic_depth/batched_reports/WS15320.html",
    "title": "WS15320 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS15320.html#cruise-ws15320",
    "href": "cruise_anoxic_depth/batched_reports/WS15320.html#cruise-ws15320",
    "title": "WS15320 Report",
    "section": "Cruise WS15320",
    "text": "Cruise WS15320\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    29             1403             44548            0.7511\n2    29             1403             44548            4.8811"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS15320.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS15320.html#anoxic-depth-map",
    "title": "WS15320 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS15208.html",
    "href": "cruise_anoxic_depth/batched_reports/WS15208.html",
    "title": "WS15208 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS15208.html#cruise-ws15208",
    "href": "cruise_anoxic_depth/batched_reports/WS15208.html#cruise-ws15208",
    "title": "WS15208 Report",
    "section": "Cruise WS15208",
    "text": "Cruise WS15208\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    46             7742             16083            1.4801\n2    46             7742             16083            5.5960"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS15208.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS15208.html#anoxic-depth-map",
    "title": "WS15208 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS15103.html",
    "href": "cruise_anoxic_depth/batched_reports/WS15103.html",
    "title": "WS15103 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS15103.html#cruise-ws15103",
    "href": "cruise_anoxic_depth/batched_reports/WS15103.html#cruise-ws15103",
    "title": "WS15103 Report",
    "section": "Cruise WS15103",
    "text": "Cruise WS15103\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    31             1718             45627           -3.5866\n2    31             1718             45627            8.2363"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS15103.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS15103.html#anoxic-depth-map",
    "title": "WS15103 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1212.html",
    "href": "cruise_anoxic_depth/batched_reports/WS1212.html",
    "title": "WS1212 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1212.html#cruise-ws1212",
    "href": "cruise_anoxic_depth/batched_reports/WS1212.html#cruise-ws1212",
    "title": "WS1212 Report",
    "section": "Cruise WS1212",
    "text": "Cruise WS1212\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    35             4944             22697            2.6969\n2    35             4944             22697            4.7057"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1212.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS1212.html#anoxic-depth-map",
    "title": "WS1212 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1202.html",
    "href": "cruise_anoxic_depth/batched_reports/WS1202.html",
    "title": "WS1202 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1202.html#cruise-ws1202",
    "href": "cruise_anoxic_depth/batched_reports/WS1202.html#cruise-ws1202",
    "title": "WS1202 Report",
    "section": "Cruise WS1202",
    "text": "Cruise WS1202\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    32             4876             25040            1.7042\n2    32             4876             25040            4.8961"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1202.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS1202.html#anoxic-depth-map",
    "title": "WS1202 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1113.html",
    "href": "cruise_anoxic_depth/batched_reports/WS1113.html",
    "title": "WS1113 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1113.html#cruise-ws1113",
    "href": "cruise_anoxic_depth/batched_reports/WS1113.html#cruise-ws1113",
    "title": "WS1113 Report",
    "section": "Cruise WS1113",
    "text": "Cruise WS1113\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    39            10050             41941            1.1610\n2    39            10050             41941            3.9663"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1113.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS1113.html#anoxic-depth-map",
    "title": "WS1113 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1102.html",
    "href": "cruise_anoxic_depth/batched_reports/WS1102.html",
    "title": "WS1102 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1102.html#cruise-ws1102",
    "href": "cruise_anoxic_depth/batched_reports/WS1102.html#cruise-ws1102",
    "title": "WS1102 Report",
    "section": "Cruise WS1102",
    "text": "Cruise WS1102\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    34             6013             21864            1.7263\n2    34             6013             21864            3.5739"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1102.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS1102.html#anoxic-depth-map",
    "title": "WS1102 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1015.html",
    "href": "cruise_anoxic_depth/batched_reports/WS1015.html",
    "title": "WS1015 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1015.html#cruise-ws1015",
    "href": "cruise_anoxic_depth/batched_reports/WS1015.html#cruise-ws1015",
    "title": "WS1015 Report",
    "section": "Cruise WS1015",
    "text": "Cruise WS1015\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    47             4760             32023            0.4466\n2    47             4760             32023            0.8162"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1015.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS1015.html#anoxic-depth-map",
    "title": "WS1015 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1004.html",
    "href": "cruise_anoxic_depth/batched_reports/WS1004.html",
    "title": "WS1004 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1004.html#cruise-ws1004",
    "href": "cruise_anoxic_depth/batched_reports/WS1004.html#cruise-ws1004",
    "title": "WS1004 Report",
    "section": "Cruise WS1004",
    "text": "Cruise WS1004\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    32             3038             34386            2.7320\n2    32             3038             34386            5.6318"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1004.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS1004.html#anoxic-depth-map",
    "title": "WS1004 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0919.html",
    "href": "cruise_anoxic_depth/batched_reports/WS0919.html",
    "title": "WS0919 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0919.html#cruise-ws0919",
    "href": "cruise_anoxic_depth/batched_reports/WS0919.html#cruise-ws0919",
    "title": "WS0919 Report",
    "section": "Cruise WS0919",
    "text": "Cruise WS0919\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    26             3063             29315            2.6507\n2    26             3063             29315            4.5363"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0919.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS0919.html#anoxic-depth-map",
    "title": "WS0919 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0906.html",
    "href": "cruise_anoxic_depth/batched_reports/WS0906.html",
    "title": "WS0906 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0906.html#cruise-ws0906",
    "href": "cruise_anoxic_depth/batched_reports/WS0906.html#cruise-ws0906",
    "title": "WS0906 Report",
    "section": "Cruise WS0906",
    "text": "Cruise WS0906\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    25             3734             28176            2.1732\n2    25             3734             28176            4.2541"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0906.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS0906.html#anoxic-depth-map",
    "title": "WS0906 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0807.html",
    "href": "cruise_anoxic_depth/batched_reports/WS0807.html",
    "title": "WS0807 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0807.html#cruise-ws0807",
    "href": "cruise_anoxic_depth/batched_reports/WS0807.html#cruise-ws0807",
    "title": "WS0807 Report",
    "section": "Cruise WS0807",
    "text": "Cruise WS0807\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    37             5543             15752             2.407\n2    37             5543             15752             5.122"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0807.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS0807.html#anoxic-depth-map",
    "title": "WS0807 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0718.html",
    "href": "cruise_anoxic_depth/batched_reports/WS0718.html",
    "title": "WS0718 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0718.html#cruise-ws0718",
    "href": "cruise_anoxic_depth/batched_reports/WS0718.html#cruise-ws0718",
    "title": "WS0718 Report",
    "section": "Cruise WS0718",
    "text": "Cruise WS0718\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    38             5163             17386           -1.2639\n2    38             5163             17386            5.1821"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0718.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS0718.html#anoxic-depth-map",
    "title": "WS0718 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0623.html",
    "href": "cruise_anoxic_depth/batched_reports/WS0623.html",
    "title": "WS0623 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0623.html#cruise-ws0623",
    "href": "cruise_anoxic_depth/batched_reports/WS0623.html#cruise-ws0623",
    "title": "WS0623 Report",
    "section": "Cruise WS0623",
    "text": "Cruise WS0623\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    33             1749             44213            1.4524\n2    33             1749             44213            3.7931"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0623.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS0623.html#anoxic-depth-map",
    "title": "WS0623 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0612.html",
    "href": "cruise_anoxic_depth/batched_reports/WS0612.html",
    "title": "WS0612 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0612.html#cruise-ws0612",
    "href": "cruise_anoxic_depth/batched_reports/WS0612.html#cruise-ws0612",
    "title": "WS0612 Report",
    "section": "Cruise WS0612",
    "text": "Cruise WS0612\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    89            14426             11875            1.8644\n2    89            14426             11875            5.6720"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0612.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS0612.html#anoxic-depth-map",
    "title": "WS0612 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WB22215.html",
    "href": "cruise_anoxic_depth/batched_reports/WB22215.html",
    "title": "WB22215 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WB22215.html#cruise-wb22215",
    "href": "cruise_anoxic_depth/batched_reports/WB22215.html#cruise-wb22215",
    "title": "WB22215 Report",
    "section": "Cruise WB22215",
    "text": "Cruise WB22215\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    58             5247             27254            1.8209\n2    58             5247             27254            5.1271"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WB22215.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WB22215.html#anoxic-depth-map",
    "title": "WB22215 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/SV18067.html",
    "href": "cruise_anoxic_depth/batched_reports/SV18067.html",
    "title": "SV18067 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/SV18067.html#cruise-sv18067",
    "href": "cruise_anoxic_depth/batched_reports/SV18067.html#cruise-sv18067",
    "title": "SV18067 Report",
    "section": "Cruise SV18067",
    "text": "Cruise SV18067\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    28             2637             42323            3.0294\n2    28             2637             42323            5.3231"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/SV18067.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/SV18067.html#anoxic-depth-map",
    "title": "SV18067 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS24139.html",
    "href": "cruise_oxygen/batched_reports/WS24139.html",
    "title": "WS24139 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS24139.html#cruise-ws24139",
    "href": "cruise_oxygen/batched_reports/WS24139.html#cruise-ws24139",
    "title": "WS24139 Report",
    "section": "Cruise WS24139",
    "text": "Cruise WS24139\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    86             7124             22680            3.1716\n2    86             7124             22680            5.6171"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS24139.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS24139.html#interpolated-field-map",
    "title": "WS24139 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS23061.html",
    "href": "cruise_oxygen/batched_reports/WS23061.html",
    "title": "WS23061 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS23061.html#cruise-ws23061",
    "href": "cruise_oxygen/batched_reports/WS23061.html#cruise-ws23061",
    "title": "WS23061 Report",
    "section": "Cruise WS23061",
    "text": "Cruise WS23061\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    86             6831             22386            2.6248\n2    86             6831             22386            5.1036"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS23061.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS23061.html#interpolated-field-map",
    "title": "WS23061 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22337.html",
    "href": "cruise_oxygen/batched_reports/WS22337.html",
    "title": "WS22337 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22337.html#cruise-ws22337",
    "href": "cruise_oxygen/batched_reports/WS22337.html#cruise-ws22337",
    "title": "WS22337 Report",
    "section": "Cruise WS22337",
    "text": "Cruise WS22337\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    79             6208             24499            2.5375\n2    79             6208             24499            6.0611"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22337.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS22337.html#interpolated-field-map",
    "title": "WS22337 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22141.html",
    "href": "cruise_oxygen/batched_reports/WS22141.html",
    "title": "WS22141 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22141.html#cruise-ws22141",
    "href": "cruise_oxygen/batched_reports/WS22141.html#cruise-ws22141",
    "title": "WS22141 Report",
    "section": "Cruise WS22141",
    "text": "Cruise WS22141\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    66             4884             25127            2.7484\n2    66             4884             25127            4.9152"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22141.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS22141.html#interpolated-field-map",
    "title": "WS22141 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22022.html",
    "href": "cruise_oxygen/batched_reports/WS22022.html",
    "title": "WS22022 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22022.html#cruise-ws22022",
    "href": "cruise_oxygen/batched_reports/WS22022.html#cruise-ws22022",
    "title": "WS22022 Report",
    "section": "Cruise WS22022",
    "text": "Cruise WS22022\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    61             4415             25523            3.1962\n2    61             4415             25523            5.1376"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22022.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS22022.html#interpolated-field-map",
    "title": "WS22022 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21278.html",
    "href": "cruise_oxygen/batched_reports/WS21278.html",
    "title": "WS21278 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21278.html#cruise-ws21278",
    "href": "cruise_oxygen/batched_reports/WS21278.html#cruise-ws21278",
    "title": "WS21278 Report",
    "section": "Cruise WS21278",
    "text": "Cruise WS21278\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    64             4508             23366            0.0640\n2    64             4508             23366            5.9163"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21278.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS21278.html#interpolated-field-map",
    "title": "WS21278 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21151.html",
    "href": "cruise_oxygen/batched_reports/WS21151.html",
    "title": "WS21151 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21151.html#cruise-ws21151",
    "href": "cruise_oxygen/batched_reports/WS21151.html#cruise-ws21151",
    "title": "WS21151 Report",
    "section": "Cruise WS21151",
    "text": "Cruise WS21151\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    73             5228             25207            2.2067\n2    73             5228             25207            4.8422"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21151.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS21151.html#interpolated-field-map",
    "title": "WS21151 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21032.html",
    "href": "cruise_oxygen/batched_reports/WS21032.html",
    "title": "WS21032 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21032.html#cruise-ws21032",
    "href": "cruise_oxygen/batched_reports/WS21032.html#cruise-ws21032",
    "title": "WS21032 Report",
    "section": "Cruise WS21032",
    "text": "Cruise WS21032\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    68             4766             22432            2.7498\n2    68             4766             22432            5.4558"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21032.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS21032.html#interpolated-field-map",
    "title": "WS21032 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS20279.html",
    "href": "cruise_oxygen/batched_reports/WS20279.html",
    "title": "WS20279 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS20279.html#cruise-ws20279",
    "href": "cruise_oxygen/batched_reports/WS20279.html#cruise-ws20279",
    "title": "WS20279 Report",
    "section": "Cruise WS20279",
    "text": "Cruise WS20279\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    64             4727             23902            2.5146\n2    64             4727             23902            4.9411"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS20279.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS20279.html#interpolated-field-map",
    "title": "WS20279 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS20006.html",
    "href": "cruise_oxygen/batched_reports/WS20006.html",
    "title": "WS20006 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS20006.html#cruise-ws20006",
    "href": "cruise_oxygen/batched_reports/WS20006.html#cruise-ws20006",
    "title": "WS20006 Report",
    "section": "Cruise WS20006",
    "text": "Cruise WS20006\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    54             4726             23554            2.6917\n2    54             4726             23554            5.2346"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS20006.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS20006.html#interpolated-field-map",
    "title": "WS20006 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19266.html",
    "href": "cruise_oxygen/batched_reports/WS19266.html",
    "title": "WS19266 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19266.html#cruise-ws19266",
    "href": "cruise_oxygen/batched_reports/WS19266.html#cruise-ws19266",
    "title": "WS19266 Report",
    "section": "Cruise WS19266",
    "text": "Cruise WS19266\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    64             5270             22003            2.7045\n2    64             5270             22003            5.3170"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19266.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS19266.html#interpolated-field-map",
    "title": "WS19266 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19119.html",
    "href": "cruise_oxygen/batched_reports/WS19119.html",
    "title": "WS19119 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19119.html#cruise-ws19119",
    "href": "cruise_oxygen/batched_reports/WS19119.html#cruise-ws19119",
    "title": "WS19119 Report",
    "section": "Cruise WS19119",
    "text": "Cruise WS19119\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    44             2435             29599            2.8489\n2    44             2435             29599            5.0629"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19119.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS19119.html#interpolated-field-map",
    "title": "WS19119 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18351.html",
    "href": "cruise_oxygen/batched_reports/WS18351.html",
    "title": "WS18351 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18351.html#cruise-ws18351",
    "href": "cruise_oxygen/batched_reports/WS18351.html#cruise-ws18351",
    "title": "WS18351 Report",
    "section": "Cruise WS18351",
    "text": "Cruise WS18351\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    12             1553             54512            3.1613\n2    12             1553             54512            4.8139"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18351.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS18351.html#interpolated-field-map",
    "title": "WS18351 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18218.html",
    "href": "cruise_oxygen/batched_reports/WS18218.html",
    "title": "WS18218 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18218.html#cruise-ws18218",
    "href": "cruise_oxygen/batched_reports/WS18218.html#cruise-ws18218",
    "title": "WS18218 Report",
    "section": "Cruise WS18218",
    "text": "Cruise WS18218\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    25             1785             45739            2.1798\n2    25             1785             45739            5.3372"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18218.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS18218.html#interpolated-field-map",
    "title": "WS18218 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18008.html",
    "href": "cruise_oxygen/batched_reports/WS18008.html",
    "title": "WS18008 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18008.html#cruise-ws18008",
    "href": "cruise_oxygen/batched_reports/WS18008.html#cruise-ws18008",
    "title": "WS18008 Report",
    "section": "Cruise WS18008",
    "text": "Cruise WS18008\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    33             1844             36649            3.7695\n2    33             1844             36649            5.9264"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18008.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS18008.html#interpolated-field-map",
    "title": "WS18008 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17212.html",
    "href": "cruise_oxygen/batched_reports/WS17212.html",
    "title": "WS17212 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17212.html#cruise-ws17212",
    "href": "cruise_oxygen/batched_reports/WS17212.html#cruise-ws17212",
    "title": "WS17212 Report",
    "section": "Cruise WS17212",
    "text": "Cruise WS17212\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    28             1883             43531            1.8337\n2    28             1883             43531            3.5272"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17212.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS17212.html#interpolated-field-map",
    "title": "WS17212 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17086.html",
    "href": "cruise_oxygen/batched_reports/WS17086.html",
    "title": "WS17086 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17086.html#cruise-ws17086",
    "href": "cruise_oxygen/batched_reports/WS17086.html#cruise-ws17086",
    "title": "WS17086 Report",
    "section": "Cruise WS17086",
    "text": "Cruise WS17086\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    33             2001             42740            3.0186\n2    33             2001             42740            5.2283"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17086.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS17086.html#interpolated-field-map",
    "title": "WS17086 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16319.html",
    "href": "cruise_oxygen/batched_reports/WS16319.html",
    "title": "WS16319 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16319.html#cruise-ws16319",
    "href": "cruise_oxygen/batched_reports/WS16319.html#cruise-ws16319",
    "title": "WS16319 Report",
    "section": "Cruise WS16319",
    "text": "Cruise WS16319\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    34             1740             45295            3.0017\n2    34             1740             45295            5.0268"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16319.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS16319.html#interpolated-field-map",
    "title": "WS16319 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16207.html",
    "href": "cruise_oxygen/batched_reports/WS16207.html",
    "title": "WS16207 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16207.html#cruise-ws16207",
    "href": "cruise_oxygen/batched_reports/WS16207.html#cruise-ws16207",
    "title": "WS16207 Report",
    "section": "Cruise WS16207",
    "text": "Cruise WS16207\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    30             1687             43753            2.9448\n2    30             1687             43753            5.1674"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16207.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS16207.html#interpolated-field-map",
    "title": "WS16207 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16004.html",
    "href": "cruise_oxygen/batched_reports/WS16004.html",
    "title": "WS16004 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16004.html#cruise-ws16004",
    "href": "cruise_oxygen/batched_reports/WS16004.html#cruise-ws16004",
    "title": "WS16004 Report",
    "section": "Cruise WS16004",
    "text": "Cruise WS16004\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    35             1789             40851            2.5802\n2    35             1789             40851            4.4590"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16004.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS16004.html#interpolated-field-map",
    "title": "WS16004 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15264.html",
    "href": "cruise_oxygen/batched_reports/WS15264.html",
    "title": "WS15264 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15264.html#cruise-ws15264",
    "href": "cruise_oxygen/batched_reports/WS15264.html#cruise-ws15264",
    "title": "WS15264 Report",
    "section": "Cruise WS15264",
    "text": "Cruise WS15264\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    36             1905             32663            2.2688\n2    36             1905             32663            4.7455"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15264.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS15264.html#interpolated-field-map",
    "title": "WS15264 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15148.html",
    "href": "cruise_oxygen/batched_reports/WS15148.html",
    "title": "WS15148 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15148.html#cruise-ws15148",
    "href": "cruise_oxygen/batched_reports/WS15148.html#cruise-ws15148",
    "title": "WS15148 Report",
    "section": "Cruise WS15148",
    "text": "Cruise WS15148\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    31             1694             47583            1.9652\n2    31             1694             47583            4.8437"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15148.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS15148.html#interpolated-field-map",
    "title": "WS15148 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS14335.html",
    "href": "cruise_oxygen/batched_reports/WS14335.html",
    "title": "WS14335 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS14335.html#cruise-ws14335",
    "href": "cruise_oxygen/batched_reports/WS14335.html#cruise-ws14335",
    "title": "WS14335 Report",
    "section": "Cruise WS14335",
    "text": "Cruise WS14335\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    36              597             80084            3.4859\n2    36              597             80084            4.9883"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS14335.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS14335.html#interpolated-field-map",
    "title": "WS14335 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1207.html",
    "href": "cruise_oxygen/batched_reports/WS1207.html",
    "title": "WS1207 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1207.html#cruise-ws1207",
    "href": "cruise_oxygen/batched_reports/WS1207.html#cruise-ws1207",
    "title": "WS1207 Report",
    "section": "Cruise WS1207",
    "text": "Cruise WS1207\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    37             4947             25484            2.1925\n2    37             4947             25484            4.1931"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1207.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS1207.html#interpolated-field-map",
    "title": "WS1207 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1116.html",
    "href": "cruise_oxygen/batched_reports/WS1116.html",
    "title": "WS1116 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1116.html#cruise-ws1116",
    "href": "cruise_oxygen/batched_reports/WS1116.html#cruise-ws1116",
    "title": "WS1116 Report",
    "section": "Cruise WS1116",
    "text": "Cruise WS1116\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    24             5131             24630            2.7303\n2    24             5131             24630            4.6591"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1116.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS1116.html#interpolated-field-map",
    "title": "WS1116 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1106.html",
    "href": "cruise_oxygen/batched_reports/WS1106.html",
    "title": "WS1106 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1106.html#cruise-ws1106",
    "href": "cruise_oxygen/batched_reports/WS1106.html#cruise-ws1106",
    "title": "WS1106 Report",
    "section": "Cruise WS1106",
    "text": "Cruise WS1106\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    30             4409             22833            2.1854\n2    30             4409             22833            4.2590"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1106.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS1106.html#interpolated-field-map",
    "title": "WS1106 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1018.html",
    "href": "cruise_oxygen/batched_reports/WS1018.html",
    "title": "WS1018 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1018.html#cruise-ws1018",
    "href": "cruise_oxygen/batched_reports/WS1018.html#cruise-ws1018",
    "title": "WS1018 Report",
    "section": "Cruise WS1018",
    "text": "Cruise WS1018\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    30             4121             27483            1.1045\n2    30             4121             27483            2.0343"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1018.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS1018.html#interpolated-field-map",
    "title": "WS1018 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1007.html",
    "href": "cruise_oxygen/batched_reports/WS1007.html",
    "title": "WS1007 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1007.html#cruise-ws1007",
    "href": "cruise_oxygen/batched_reports/WS1007.html#cruise-ws1007",
    "title": "WS1007 Report",
    "section": "Cruise WS1007",
    "text": "Cruise WS1007\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    35             4131             24248            2.7489\n2    35             4131             24248            5.5538"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1007.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS1007.html#interpolated-field-map",
    "title": "WS1007 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0923.html",
    "href": "cruise_oxygen/batched_reports/WS0923.html",
    "title": "WS0923 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0923.html#cruise-ws0923",
    "href": "cruise_oxygen/batched_reports/WS0923.html#cruise-ws0923",
    "title": "WS0923 Report",
    "section": "Cruise WS0923",
    "text": "Cruise WS0923\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    38             4491             31340            2.7817\n2    38             4491             31340            5.2614"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0923.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS0923.html#interpolated-field-map",
    "title": "WS0923 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0914.html",
    "href": "cruise_oxygen/batched_reports/WS0914.html",
    "title": "WS0914 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0914.html#cruise-ws0914",
    "href": "cruise_oxygen/batched_reports/WS0914.html#cruise-ws0914",
    "title": "WS0914 Report",
    "section": "Cruise WS0914",
    "text": "Cruise WS0914\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    26             4638             24645            2.3144\n2    26             4638             24645            4.6715"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0914.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS0914.html#interpolated-field-map",
    "title": "WS0914 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0901.html",
    "href": "cruise_oxygen/batched_reports/WS0901.html",
    "title": "WS0901 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0901.html#cruise-ws0901",
    "href": "cruise_oxygen/batched_reports/WS0901.html#cruise-ws0901",
    "title": "WS0901 Report",
    "section": "Cruise WS0901",
    "text": "Cruise WS0901\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    39             5124             18775            2.6342\n2    39             5124             18775            5.5332"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0901.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS0901.html#interpolated-field-map",
    "title": "WS0901 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0802.html",
    "href": "cruise_oxygen/batched_reports/WS0802.html",
    "title": "WS0802 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0802.html#cruise-ws0802",
    "href": "cruise_oxygen/batched_reports/WS0802.html#cruise-ws0802",
    "title": "WS0802 Report",
    "section": "Cruise WS0802",
    "text": "Cruise WS0802\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    36             5412             17392            2.6808\n2    36             5412             17392            5.2205"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0802.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS0802.html#interpolated-field-map",
    "title": "WS0802 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0704.html",
    "href": "cruise_oxygen/batched_reports/WS0704.html",
    "title": "WS0704 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0704.html#cruise-ws0704",
    "href": "cruise_oxygen/batched_reports/WS0704.html#cruise-ws0704",
    "title": "WS0704 Report",
    "section": "Cruise WS0704",
    "text": "Cruise WS0704\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    34             4981             18166            1.8837\n2    34             4981             18166            4.0938"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0704.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS0704.html#interpolated-field-map",
    "title": "WS0704 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0618.html",
    "href": "cruise_oxygen/batched_reports/WS0618.html",
    "title": "WS0618 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0618.html#cruise-ws0618",
    "href": "cruise_oxygen/batched_reports/WS0618.html#cruise-ws0618",
    "title": "WS0618 Report",
    "section": "Cruise WS0618",
    "text": "Cruise WS0618\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    49             9332             12363            1.8812\n2    49             9332             12363            4.6943"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0618.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS0618.html#interpolated-field-map",
    "title": "WS0618 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0603.html",
    "href": "cruise_oxygen/batched_reports/WS0603.html",
    "title": "WS0603 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0603.html#cruise-ws0603",
    "href": "cruise_oxygen/batched_reports/WS0603.html#cruise-ws0603",
    "title": "WS0603 Report",
    "section": "Cruise WS0603",
    "text": "Cruise WS0603\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    95            13121             12090            1.8824\n2    95            13121             12090            4.0451"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0603.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS0603.html#interpolated-field-map",
    "title": "WS0603 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/SV18173.html",
    "href": "cruise_oxygen/batched_reports/SV18173.html",
    "title": "SV18173 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/SV18173.html#cruise-sv18173",
    "href": "cruise_oxygen/batched_reports/SV18173.html#cruise-sv18173",
    "title": "SV18173 Report",
    "section": "Cruise SV18173",
    "text": "Cruise SV18173\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    31             1945             45901            2.6239\n2    31             1945             45901            5.1915"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/SV18173.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/SV18173.html#interpolated-field-map",
    "title": "SV18173 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "index.html",
    "href": "index.html",
    "title": "SFER MBON Oxygen",
    "section": "",
    "text": "Cruise reports generated from cleaned CTD data and DIVAnd-interpolated dissolved oxygen fields.\n\nOxygen field maps\nAnoxic depth maps"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/SV18067.html",
    "href": "cruise_oxygen/batched_reports/SV18067.html",
    "title": "SV18067 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/SV18067.html#cruise-sv18067",
    "href": "cruise_oxygen/batched_reports/SV18067.html#cruise-sv18067",
    "title": "SV18067 Report",
    "section": "Cruise SV18067",
    "text": "Cruise SV18067\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    28             2637             42323            3.0294\n2    28             2637             42323            5.3231"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/SV18067.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/SV18067.html#interpolated-field-map",
    "title": "SV18067 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WB22215.html",
    "href": "cruise_oxygen/batched_reports/WB22215.html",
    "title": "WB22215 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WB22215.html#cruise-wb22215",
    "href": "cruise_oxygen/batched_reports/WB22215.html#cruise-wb22215",
    "title": "WB22215 Report",
    "section": "Cruise WB22215",
    "text": "Cruise WB22215\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    58             5247             27254            1.8209\n2    58             5247             27254            5.1271"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WB22215.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WB22215.html#interpolated-field-map",
    "title": "WB22215 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0612.html",
    "href": "cruise_oxygen/batched_reports/WS0612.html",
    "title": "WS0612 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0612.html#cruise-ws0612",
    "href": "cruise_oxygen/batched_reports/WS0612.html#cruise-ws0612",
    "title": "WS0612 Report",
    "section": "Cruise WS0612",
    "text": "Cruise WS0612\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    89            14426             11875            1.8644\n2    89            14426             11875            5.6720"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0612.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS0612.html#interpolated-field-map",
    "title": "WS0612 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0623.html",
    "href": "cruise_oxygen/batched_reports/WS0623.html",
    "title": "WS0623 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0623.html#cruise-ws0623",
    "href": "cruise_oxygen/batched_reports/WS0623.html#cruise-ws0623",
    "title": "WS0623 Report",
    "section": "Cruise WS0623",
    "text": "Cruise WS0623\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    33             1749             44213            1.4524\n2    33             1749             44213            3.7931"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0623.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS0623.html#interpolated-field-map",
    "title": "WS0623 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0718.html",
    "href": "cruise_oxygen/batched_reports/WS0718.html",
    "title": "WS0718 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0718.html#cruise-ws0718",
    "href": "cruise_oxygen/batched_reports/WS0718.html#cruise-ws0718",
    "title": "WS0718 Report",
    "section": "Cruise WS0718",
    "text": "Cruise WS0718\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    38             5163             17386           -1.2639\n2    38             5163             17386            5.1821"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0718.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS0718.html#interpolated-field-map",
    "title": "WS0718 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0807.html",
    "href": "cruise_oxygen/batched_reports/WS0807.html",
    "title": "WS0807 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0807.html#cruise-ws0807",
    "href": "cruise_oxygen/batched_reports/WS0807.html#cruise-ws0807",
    "title": "WS0807 Report",
    "section": "Cruise WS0807",
    "text": "Cruise WS0807\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    37             5543             15752             2.407\n2    37             5543             15752             5.122"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0807.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS0807.html#interpolated-field-map",
    "title": "WS0807 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0906.html",
    "href": "cruise_oxygen/batched_reports/WS0906.html",
    "title": "WS0906 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0906.html#cruise-ws0906",
    "href": "cruise_oxygen/batched_reports/WS0906.html#cruise-ws0906",
    "title": "WS0906 Report",
    "section": "Cruise WS0906",
    "text": "Cruise WS0906\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    25             3734             28176            2.1732\n2    25             3734             28176            4.2541"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0906.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS0906.html#interpolated-field-map",
    "title": "WS0906 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0919.html",
    "href": "cruise_oxygen/batched_reports/WS0919.html",
    "title": "WS0919 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0919.html#cruise-ws0919",
    "href": "cruise_oxygen/batched_reports/WS0919.html#cruise-ws0919",
    "title": "WS0919 Report",
    "section": "Cruise WS0919",
    "text": "Cruise WS0919\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    26             3063             29315            2.6507\n2    26             3063             29315            4.5363"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0919.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS0919.html#interpolated-field-map",
    "title": "WS0919 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1004.html",
    "href": "cruise_oxygen/batched_reports/WS1004.html",
    "title": "WS1004 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1004.html#cruise-ws1004",
    "href": "cruise_oxygen/batched_reports/WS1004.html#cruise-ws1004",
    "title": "WS1004 Report",
    "section": "Cruise WS1004",
    "text": "Cruise WS1004\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    32             3038             34386            2.7320\n2    32             3038             34386            5.6318"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1004.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS1004.html#interpolated-field-map",
    "title": "WS1004 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1015.html",
    "href": "cruise_oxygen/batched_reports/WS1015.html",
    "title": "WS1015 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1015.html#cruise-ws1015",
    "href": "cruise_oxygen/batched_reports/WS1015.html#cruise-ws1015",
    "title": "WS1015 Report",
    "section": "Cruise WS1015",
    "text": "Cruise WS1015\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    47             4760             32023            0.4466\n2    47             4760             32023            0.8162"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1015.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS1015.html#interpolated-field-map",
    "title": "WS1015 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1102.html",
    "href": "cruise_oxygen/batched_reports/WS1102.html",
    "title": "WS1102 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1102.html#cruise-ws1102",
    "href": "cruise_oxygen/batched_reports/WS1102.html#cruise-ws1102",
    "title": "WS1102 Report",
    "section": "Cruise WS1102",
    "text": "Cruise WS1102\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    34             6013             21864            1.7263\n2    34             6013             21864            3.5739"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1102.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS1102.html#interpolated-field-map",
    "title": "WS1102 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1113.html",
    "href": "cruise_oxygen/batched_reports/WS1113.html",
    "title": "WS1113 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1113.html#cruise-ws1113",
    "href": "cruise_oxygen/batched_reports/WS1113.html#cruise-ws1113",
    "title": "WS1113 Report",
    "section": "Cruise WS1113",
    "text": "Cruise WS1113\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    39            10050             41941            1.1610\n2    39            10050             41941            3.9663"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1113.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS1113.html#interpolated-field-map",
    "title": "WS1113 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1202.html",
    "href": "cruise_oxygen/batched_reports/WS1202.html",
    "title": "WS1202 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1202.html#cruise-ws1202",
    "href": "cruise_oxygen/batched_reports/WS1202.html#cruise-ws1202",
    "title": "WS1202 Report",
    "section": "Cruise WS1202",
    "text": "Cruise WS1202\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    32             4876             25040            1.7042\n2    32             4876             25040            4.8961"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1202.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS1202.html#interpolated-field-map",
    "title": "WS1202 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1212.html",
    "href": "cruise_oxygen/batched_reports/WS1212.html",
    "title": "WS1212 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1212.html#cruise-ws1212",
    "href": "cruise_oxygen/batched_reports/WS1212.html#cruise-ws1212",
    "title": "WS1212 Report",
    "section": "Cruise WS1212",
    "text": "Cruise WS1212\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    35             4944             22697            2.6969\n2    35             4944             22697            4.7057"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1212.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS1212.html#interpolated-field-map",
    "title": "WS1212 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15103.html",
    "href": "cruise_oxygen/batched_reports/WS15103.html",
    "title": "WS15103 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15103.html#cruise-ws15103",
    "href": "cruise_oxygen/batched_reports/WS15103.html#cruise-ws15103",
    "title": "WS15103 Report",
    "section": "Cruise WS15103",
    "text": "Cruise WS15103\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    31             1718             45627           -3.5866\n2    31             1718             45627            8.2363"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15103.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS15103.html#interpolated-field-map",
    "title": "WS15103 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15208.html",
    "href": "cruise_oxygen/batched_reports/WS15208.html",
    "title": "WS15208 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15208.html#cruise-ws15208",
    "href": "cruise_oxygen/batched_reports/WS15208.html#cruise-ws15208",
    "title": "WS15208 Report",
    "section": "Cruise WS15208",
    "text": "Cruise WS15208\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    46             7742             16083            1.4801\n2    46             7742             16083            5.5960"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15208.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS15208.html#interpolated-field-map",
    "title": "WS15208 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15320.html",
    "href": "cruise_oxygen/batched_reports/WS15320.html",
    "title": "WS15320 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15320.html#cruise-ws15320",
    "href": "cruise_oxygen/batched_reports/WS15320.html#cruise-ws15320",
    "title": "WS15320 Report",
    "section": "Cruise WS15320",
    "text": "Cruise WS15320\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    29             1403             44548            0.7511\n2    29             1403             44548            4.8811"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15320.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS15320.html#interpolated-field-map",
    "title": "WS15320 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16074.html",
    "href": "cruise_oxygen/batched_reports/WS16074.html",
    "title": "WS16074 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16074.html#cruise-ws16074",
    "href": "cruise_oxygen/batched_reports/WS16074.html#cruise-ws16074",
    "title": "WS16074 Report",
    "section": "Cruise WS16074",
    "text": "Cruise WS16074\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    29             1152             72889            2.5237\n2    29             1152             72889            5.5489"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16074.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS16074.html#interpolated-field-map",
    "title": "WS16074 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16263.html",
    "href": "cruise_oxygen/batched_reports/WS16263.html",
    "title": "WS16263 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16263.html#cruise-ws16263",
    "href": "cruise_oxygen/batched_reports/WS16263.html#cruise-ws16263",
    "title": "WS16263 Report",
    "section": "Cruise WS16263",
    "text": "Cruise WS16263\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    29             1610             42306            2.0717\n2    29             1610             42306            4.9545"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16263.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS16263.html#interpolated-field-map",
    "title": "WS16263 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17030.html",
    "href": "cruise_oxygen/batched_reports/WS17030.html",
    "title": "WS17030 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17030.html#cruise-ws17030",
    "href": "cruise_oxygen/batched_reports/WS17030.html#cruise-ws17030",
    "title": "WS17030 Report",
    "section": "Cruise WS17030",
    "text": "Cruise WS17030\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    30             1833             43764            2.8508\n2    30             1833             43764            5.6084"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17030.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS17030.html#interpolated-field-map",
    "title": "WS17030 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17170.html",
    "href": "cruise_oxygen/batched_reports/WS17170.html",
    "title": "WS17170 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17170.html#cruise-ws17170",
    "href": "cruise_oxygen/batched_reports/WS17170.html#cruise-ws17170",
    "title": "WS17170 Report",
    "section": "Cruise WS17170",
    "text": "Cruise WS17170\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    25              486             89436            2.8004\n2    25              486             89436            4.4252"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17170.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS17170.html#interpolated-field-map",
    "title": "WS17170 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17282.html",
    "href": "cruise_oxygen/batched_reports/WS17282.html",
    "title": "WS17282 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17282.html#cruise-ws17282",
    "href": "cruise_oxygen/batched_reports/WS17282.html#cruise-ws17282",
    "title": "WS17282 Report",
    "section": "Cruise WS17282",
    "text": "Cruise WS17282\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    63             4745             48832            2.7664\n2    63             4745             48832            5.0644"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17282.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS17282.html#interpolated-field-map",
    "title": "WS17282 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18120.html",
    "href": "cruise_oxygen/batched_reports/WS18120.html",
    "title": "WS18120 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18120.html#cruise-ws18120",
    "href": "cruise_oxygen/batched_reports/WS18120.html#cruise-ws18120",
    "title": "WS18120 Report",
    "section": "Cruise WS18120",
    "text": "Cruise WS18120\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    19              933             60286            2.6945\n2    19              933             60286            4.9503"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18120.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS18120.html#interpolated-field-map",
    "title": "WS18120 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18285.html",
    "href": "cruise_oxygen/batched_reports/WS18285.html",
    "title": "WS18285 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18285.html#cruise-ws18285",
    "href": "cruise_oxygen/batched_reports/WS18285.html#cruise-ws18285",
    "title": "WS18285 Report",
    "section": "Cruise WS18285",
    "text": "Cruise WS18285\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    64             4324             24152            1.1042\n2    64             4324             24152            8.5705"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18285.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS18285.html#interpolated-field-map",
    "title": "WS18285 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19028.html",
    "href": "cruise_oxygen/batched_reports/WS19028.html",
    "title": "WS19028 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19028.html#cruise-ws19028",
    "href": "cruise_oxygen/batched_reports/WS19028.html#cruise-ws19028",
    "title": "WS19028 Report",
    "section": "Cruise WS19028",
    "text": "Cruise WS19028\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    33             1345             62034            3.2756\n2    33             1345             62034            5.7658"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19028.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS19028.html#interpolated-field-map",
    "title": "WS19028 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19210.html",
    "href": "cruise_oxygen/batched_reports/WS19210.html",
    "title": "WS19210 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19210.html#cruise-ws19210",
    "href": "cruise_oxygen/batched_reports/WS19210.html#cruise-ws19210",
    "title": "WS19210 Report",
    "section": "Cruise WS19210",
    "text": "Cruise WS19210\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    42             2486             29440            2.5009\n2    42             2486             29440            5.1375"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19210.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS19210.html#interpolated-field-map",
    "title": "WS19210 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19322.html",
    "href": "cruise_oxygen/batched_reports/WS19322.html",
    "title": "WS19322 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19322.html#cruise-ws19322",
    "href": "cruise_oxygen/batched_reports/WS19322.html#cruise-ws19322",
    "title": "WS19322 Report",
    "section": "Cruise WS19322",
    "text": "Cruise WS19322\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    70             4557             24279            3.2941\n2    70             4557             24279            5.8361"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19322.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS19322.html#interpolated-field-map",
    "title": "WS19322 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS20231.html",
    "href": "cruise_oxygen/batched_reports/WS20231.html",
    "title": "WS20231 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS20231.html#cruise-ws20231",
    "href": "cruise_oxygen/batched_reports/WS20231.html#cruise-ws20231",
    "title": "WS20231 Report",
    "section": "Cruise WS20231",
    "text": "Cruise WS20231\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    43             3054             30048           -1.0535\n2    43             3054             30048            4.6257"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS20231.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS20231.html#interpolated-field-map",
    "title": "WS20231 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS20342.html",
    "href": "cruise_oxygen/batched_reports/WS20342.html",
    "title": "WS20342 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS20342.html#cruise-ws20342",
    "href": "cruise_oxygen/batched_reports/WS20342.html#cruise-ws20342",
    "title": "WS20342 Report",
    "section": "Cruise WS20342",
    "text": "Cruise WS20342\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    67             4503             24573            2.7412\n2    67             4503             24573            5.1600"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS20342.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS20342.html#interpolated-field-map",
    "title": "WS20342 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21093.html",
    "href": "cruise_oxygen/batched_reports/WS21093.html",
    "title": "WS21093 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21093.html#cruise-ws21093",
    "href": "cruise_oxygen/batched_reports/WS21093.html#cruise-ws21093",
    "title": "WS21093 Report",
    "section": "Cruise WS21093",
    "text": "Cruise WS21093\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    64             4050             53252         -104.1946\n2    64             4050             53252            5.2020"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21093.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS21093.html#interpolated-field-map",
    "title": "WS21093 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21212.html",
    "href": "cruise_oxygen/batched_reports/WS21212.html",
    "title": "WS21212 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21212.html#cruise-ws21212",
    "href": "cruise_oxygen/batched_reports/WS21212.html#cruise-ws21212",
    "title": "WS21212 Report",
    "section": "Cruise WS21212",
    "text": "Cruise WS21212\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    70             4284             29480            1.4935\n2    70             4284             29480            4.5662"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21212.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS21212.html#interpolated-field-map",
    "title": "WS21212 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21338.html",
    "href": "cruise_oxygen/batched_reports/WS21338.html",
    "title": "WS21338 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21338.html#cruise-ws21338",
    "href": "cruise_oxygen/batched_reports/WS21338.html#cruise-ws21338",
    "title": "WS21338 Report",
    "section": "Cruise WS21338",
    "text": "Cruise WS21338\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    71             5691             25855            2.4974\n2    71             5691             25855            5.5520"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21338.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS21338.html#interpolated-field-map",
    "title": "WS21338 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22072.html",
    "href": "cruise_oxygen/batched_reports/WS22072.html",
    "title": "WS22072 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22072.html#cruise-ws22072",
    "href": "cruise_oxygen/batched_reports/WS22072.html#cruise-ws22072",
    "title": "WS22072 Report",
    "section": "Cruise WS22072",
    "text": "Cruise WS22072\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    76             6530             23007            2.6545\n2    76             6530             23007            5.1897"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22072.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS22072.html#interpolated-field-map",
    "title": "WS22072 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22281.html",
    "href": "cruise_oxygen/batched_reports/WS22281.html",
    "title": "WS22281 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22281.html#cruise-ws22281",
    "href": "cruise_oxygen/batched_reports/WS22281.html#cruise-ws22281",
    "title": "WS22281 Report",
    "section": "Cruise WS22281",
    "text": "Cruise WS22281\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    73             5431             24969            2.7499\n2    73             5431             24969            4.8476"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22281.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS22281.html#interpolated-field-map",
    "title": "WS22281 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS23011.html",
    "href": "cruise_oxygen/batched_reports/WS23011.html",
    "title": "WS23011 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS23011.html#cruise-ws23011",
    "href": "cruise_oxygen/batched_reports/WS23011.html#cruise-ws23011",
    "title": "WS23011 Report",
    "section": "Cruise WS23011",
    "text": "Cruise WS23011\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    73             4505             25994            3.1448\n2    73             4505             25994            5.6046"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS23011.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS23011.html#interpolated-field-map",
    "title": "WS23011 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS23203.html",
    "href": "cruise_oxygen/batched_reports/WS23203.html",
    "title": "WS23203 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS23203.html#cruise-ws23203",
    "href": "cruise_oxygen/batched_reports/WS23203.html#cruise-ws23203",
    "title": "WS23203 Report",
    "section": "Cruise WS23203",
    "text": "Cruise WS23203\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    84             6994             23696            1.3486\n2    84             6994             23696            5.9850"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS23203.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS23203.html#interpolated-field-map",
    "title": "WS23203 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next to step through depth bands. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/listing.html",
    "href": "cruise_oxygen/listing.html",
    "title": "cruise_oxygen",
    "section": "",
    "text": "Click on any element for more details.\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nSV18067 Report\n\n\n\n\n\n\n\n\n\n\n\n\nSV18173 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWB22215 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0603 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0612 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0618 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0623 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0704 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0718 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0802 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0807 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0901 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0906 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0914 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0919 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0923 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1004 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1007 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1015 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1018 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1102 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1106 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1113 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1116 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1202 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1207 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1212 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS14335 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS15103 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS15148 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS15208 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS15264 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS15320 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS16004 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS16074 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS16207 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS16263 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS16319 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS17030 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS17086 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS17170 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS17212 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS17282 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS18008 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS18120 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS18218 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS18285 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS18351 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS19028 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS19119 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS19210 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS19266 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS19322 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS20006 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS20231 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS20279 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS20342 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS21032 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS21093 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS21151 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS21212 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS21278 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS21338 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS22022 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS22072 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS22141 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS22281 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS22337 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS23011 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS23061 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS23203 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS24139 Report\n\n\n\n\n\nNo matching items"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/SV18173.html",
    "href": "cruise_anoxic_depth/batched_reports/SV18173.html",
    "title": "SV18173 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/SV18173.html#cruise-sv18173",
    "href": "cruise_anoxic_depth/batched_reports/SV18173.html#cruise-sv18173",
    "title": "SV18173 Report",
    "section": "Cruise SV18173",
    "text": "Cruise SV18173\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    31             1945             45901            2.6239\n2    31             1945             45901            5.1915"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/SV18173.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/SV18173.html#anoxic-depth-map",
    "title": "SV18173 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0603.html",
    "href": "cruise_anoxic_depth/batched_reports/WS0603.html",
    "title": "WS0603 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0603.html#cruise-ws0603",
    "href": "cruise_anoxic_depth/batched_reports/WS0603.html#cruise-ws0603",
    "title": "WS0603 Report",
    "section": "Cruise WS0603",
    "text": "Cruise WS0603\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    95            13121             12090            1.8824\n2    95            13121             12090            4.0451"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0603.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS0603.html#anoxic-depth-map",
    "title": "WS0603 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0618.html",
    "href": "cruise_anoxic_depth/batched_reports/WS0618.html",
    "title": "WS0618 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0618.html#cruise-ws0618",
    "href": "cruise_anoxic_depth/batched_reports/WS0618.html#cruise-ws0618",
    "title": "WS0618 Report",
    "section": "Cruise WS0618",
    "text": "Cruise WS0618\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    49             9332             12363            1.8812\n2    49             9332             12363            4.6943"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0618.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS0618.html#anoxic-depth-map",
    "title": "WS0618 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0704.html",
    "href": "cruise_anoxic_depth/batched_reports/WS0704.html",
    "title": "WS0704 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0704.html#cruise-ws0704",
    "href": "cruise_anoxic_depth/batched_reports/WS0704.html#cruise-ws0704",
    "title": "WS0704 Report",
    "section": "Cruise WS0704",
    "text": "Cruise WS0704\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    34             4981             18166            1.8837\n2    34             4981             18166            4.0938"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0704.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS0704.html#anoxic-depth-map",
    "title": "WS0704 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0802.html",
    "href": "cruise_anoxic_depth/batched_reports/WS0802.html",
    "title": "WS0802 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0802.html#cruise-ws0802",
    "href": "cruise_anoxic_depth/batched_reports/WS0802.html#cruise-ws0802",
    "title": "WS0802 Report",
    "section": "Cruise WS0802",
    "text": "Cruise WS0802\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    36             5412             17392            2.6808\n2    36             5412             17392            5.2205"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0802.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS0802.html#anoxic-depth-map",
    "title": "WS0802 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0901.html",
    "href": "cruise_anoxic_depth/batched_reports/WS0901.html",
    "title": "WS0901 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0901.html#cruise-ws0901",
    "href": "cruise_anoxic_depth/batched_reports/WS0901.html#cruise-ws0901",
    "title": "WS0901 Report",
    "section": "Cruise WS0901",
    "text": "Cruise WS0901\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    39             5124             18775            2.6342\n2    39             5124             18775            5.5332"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0901.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS0901.html#anoxic-depth-map",
    "title": "WS0901 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0914.html",
    "href": "cruise_anoxic_depth/batched_reports/WS0914.html",
    "title": "WS0914 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0914.html#cruise-ws0914",
    "href": "cruise_anoxic_depth/batched_reports/WS0914.html#cruise-ws0914",
    "title": "WS0914 Report",
    "section": "Cruise WS0914",
    "text": "Cruise WS0914\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    26             4638             24645            2.3144\n2    26             4638             24645            4.6715"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0914.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS0914.html#anoxic-depth-map",
    "title": "WS0914 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0923.html",
    "href": "cruise_anoxic_depth/batched_reports/WS0923.html",
    "title": "WS0923 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0923.html#cruise-ws0923",
    "href": "cruise_anoxic_depth/batched_reports/WS0923.html#cruise-ws0923",
    "title": "WS0923 Report",
    "section": "Cruise WS0923",
    "text": "Cruise WS0923\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    38             4491             31340            2.7817\n2    38             4491             31340            5.2614"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS0923.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS0923.html#anoxic-depth-map",
    "title": "WS0923 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1007.html",
    "href": "cruise_anoxic_depth/batched_reports/WS1007.html",
    "title": "WS1007 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1007.html#cruise-ws1007",
    "href": "cruise_anoxic_depth/batched_reports/WS1007.html#cruise-ws1007",
    "title": "WS1007 Report",
    "section": "Cruise WS1007",
    "text": "Cruise WS1007\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    35             4131             24248            2.7489\n2    35             4131             24248            5.5538"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1007.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS1007.html#anoxic-depth-map",
    "title": "WS1007 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1018.html",
    "href": "cruise_anoxic_depth/batched_reports/WS1018.html",
    "title": "WS1018 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1018.html#cruise-ws1018",
    "href": "cruise_anoxic_depth/batched_reports/WS1018.html#cruise-ws1018",
    "title": "WS1018 Report",
    "section": "Cruise WS1018",
    "text": "Cruise WS1018\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    30             4121             27483            1.1045\n2    30             4121             27483            2.0343"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1018.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS1018.html#anoxic-depth-map",
    "title": "WS1018 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1106.html",
    "href": "cruise_anoxic_depth/batched_reports/WS1106.html",
    "title": "WS1106 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1106.html#cruise-ws1106",
    "href": "cruise_anoxic_depth/batched_reports/WS1106.html#cruise-ws1106",
    "title": "WS1106 Report",
    "section": "Cruise WS1106",
    "text": "Cruise WS1106\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    30             4409             22833            2.1854\n2    30             4409             22833            4.2590"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1106.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS1106.html#anoxic-depth-map",
    "title": "WS1106 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1116.html",
    "href": "cruise_anoxic_depth/batched_reports/WS1116.html",
    "title": "WS1116 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1116.html#cruise-ws1116",
    "href": "cruise_anoxic_depth/batched_reports/WS1116.html#cruise-ws1116",
    "title": "WS1116 Report",
    "section": "Cruise WS1116",
    "text": "Cruise WS1116\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    24             5131             24630            2.7303\n2    24             5131             24630            4.6591"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1116.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS1116.html#anoxic-depth-map",
    "title": "WS1116 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1207.html",
    "href": "cruise_anoxic_depth/batched_reports/WS1207.html",
    "title": "WS1207 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1207.html#cruise-ws1207",
    "href": "cruise_anoxic_depth/batched_reports/WS1207.html#cruise-ws1207",
    "title": "WS1207 Report",
    "section": "Cruise WS1207",
    "text": "Cruise WS1207\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    37             4947             25484            2.1925\n2    37             4947             25484            4.1931"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS1207.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS1207.html#anoxic-depth-map",
    "title": "WS1207 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS14335.html",
    "href": "cruise_anoxic_depth/batched_reports/WS14335.html",
    "title": "WS14335 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS14335.html#cruise-ws14335",
    "href": "cruise_anoxic_depth/batched_reports/WS14335.html#cruise-ws14335",
    "title": "WS14335 Report",
    "section": "Cruise WS14335",
    "text": "Cruise WS14335\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    36              597             80084            3.4859\n2    36              597             80084            4.9883"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS14335.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS14335.html#anoxic-depth-map",
    "title": "WS14335 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS15148.html",
    "href": "cruise_anoxic_depth/batched_reports/WS15148.html",
    "title": "WS15148 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS15148.html#cruise-ws15148",
    "href": "cruise_anoxic_depth/batched_reports/WS15148.html#cruise-ws15148",
    "title": "WS15148 Report",
    "section": "Cruise WS15148",
    "text": "Cruise WS15148\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    31             1694             47583            1.9652\n2    31             1694             47583            4.8437"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS15148.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS15148.html#anoxic-depth-map",
    "title": "WS15148 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS15264.html",
    "href": "cruise_anoxic_depth/batched_reports/WS15264.html",
    "title": "WS15264 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS15264.html#cruise-ws15264",
    "href": "cruise_anoxic_depth/batched_reports/WS15264.html#cruise-ws15264",
    "title": "WS15264 Report",
    "section": "Cruise WS15264",
    "text": "Cruise WS15264\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    36             1905             32663            2.2688\n2    36             1905             32663            4.7455"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS15264.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS15264.html#anoxic-depth-map",
    "title": "WS15264 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS16004.html",
    "href": "cruise_anoxic_depth/batched_reports/WS16004.html",
    "title": "WS16004 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS16004.html#cruise-ws16004",
    "href": "cruise_anoxic_depth/batched_reports/WS16004.html#cruise-ws16004",
    "title": "WS16004 Report",
    "section": "Cruise WS16004",
    "text": "Cruise WS16004\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    35             1789             40851            2.5802\n2    35             1789             40851            4.4590"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS16004.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS16004.html#anoxic-depth-map",
    "title": "WS16004 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS16207.html",
    "href": "cruise_anoxic_depth/batched_reports/WS16207.html",
    "title": "WS16207 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS16207.html#cruise-ws16207",
    "href": "cruise_anoxic_depth/batched_reports/WS16207.html#cruise-ws16207",
    "title": "WS16207 Report",
    "section": "Cruise WS16207",
    "text": "Cruise WS16207\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    30             1687             43753            2.9448\n2    30             1687             43753            5.1674"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS16207.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS16207.html#anoxic-depth-map",
    "title": "WS16207 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS16319.html",
    "href": "cruise_anoxic_depth/batched_reports/WS16319.html",
    "title": "WS16319 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS16319.html#cruise-ws16319",
    "href": "cruise_anoxic_depth/batched_reports/WS16319.html#cruise-ws16319",
    "title": "WS16319 Report",
    "section": "Cruise WS16319",
    "text": "Cruise WS16319\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    34             1740             45295            3.0017\n2    34             1740             45295            5.0268"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS16319.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS16319.html#anoxic-depth-map",
    "title": "WS16319 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS17086.html",
    "href": "cruise_anoxic_depth/batched_reports/WS17086.html",
    "title": "WS17086 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS17086.html#cruise-ws17086",
    "href": "cruise_anoxic_depth/batched_reports/WS17086.html#cruise-ws17086",
    "title": "WS17086 Report",
    "section": "Cruise WS17086",
    "text": "Cruise WS17086\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    33             2001             42740            3.0186\n2    33             2001             42740            5.2283"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS17086.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS17086.html#anoxic-depth-map",
    "title": "WS17086 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS17212.html",
    "href": "cruise_anoxic_depth/batched_reports/WS17212.html",
    "title": "WS17212 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS17212.html#cruise-ws17212",
    "href": "cruise_anoxic_depth/batched_reports/WS17212.html#cruise-ws17212",
    "title": "WS17212 Report",
    "section": "Cruise WS17212",
    "text": "Cruise WS17212\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    28             1883             43531            1.8337\n2    28             1883             43531            3.5272"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS17212.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS17212.html#anoxic-depth-map",
    "title": "WS17212 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS18008.html",
    "href": "cruise_anoxic_depth/batched_reports/WS18008.html",
    "title": "WS18008 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS18008.html#cruise-ws18008",
    "href": "cruise_anoxic_depth/batched_reports/WS18008.html#cruise-ws18008",
    "title": "WS18008 Report",
    "section": "Cruise WS18008",
    "text": "Cruise WS18008\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    33             1844             36649            3.7695\n2    33             1844             36649            5.9264"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS18008.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS18008.html#anoxic-depth-map",
    "title": "WS18008 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS18218.html",
    "href": "cruise_anoxic_depth/batched_reports/WS18218.html",
    "title": "WS18218 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS18218.html#cruise-ws18218",
    "href": "cruise_anoxic_depth/batched_reports/WS18218.html#cruise-ws18218",
    "title": "WS18218 Report",
    "section": "Cruise WS18218",
    "text": "Cruise WS18218\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    25             1785             45739            2.1798\n2    25             1785             45739            5.3372"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS18218.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS18218.html#anoxic-depth-map",
    "title": "WS18218 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS18351.html",
    "href": "cruise_anoxic_depth/batched_reports/WS18351.html",
    "title": "WS18351 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS18351.html#cruise-ws18351",
    "href": "cruise_anoxic_depth/batched_reports/WS18351.html#cruise-ws18351",
    "title": "WS18351 Report",
    "section": "Cruise WS18351",
    "text": "Cruise WS18351\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    12             1553             54512            3.1613\n2    12             1553             54512            4.8139"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS18351.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS18351.html#anoxic-depth-map",
    "title": "WS18351 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS19119.html",
    "href": "cruise_anoxic_depth/batched_reports/WS19119.html",
    "title": "WS19119 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS19119.html#cruise-ws19119",
    "href": "cruise_anoxic_depth/batched_reports/WS19119.html#cruise-ws19119",
    "title": "WS19119 Report",
    "section": "Cruise WS19119",
    "text": "Cruise WS19119\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    44             2435             29599            2.8489\n2    44             2435             29599            5.0629"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS19119.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS19119.html#anoxic-depth-map",
    "title": "WS19119 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS19266.html",
    "href": "cruise_anoxic_depth/batched_reports/WS19266.html",
    "title": "WS19266 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS19266.html#cruise-ws19266",
    "href": "cruise_anoxic_depth/batched_reports/WS19266.html#cruise-ws19266",
    "title": "WS19266 Report",
    "section": "Cruise WS19266",
    "text": "Cruise WS19266\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    64             5270             22003            2.7045\n2    64             5270             22003            5.3170"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS19266.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS19266.html#anoxic-depth-map",
    "title": "WS19266 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS20006.html",
    "href": "cruise_anoxic_depth/batched_reports/WS20006.html",
    "title": "WS20006 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS20006.html#cruise-ws20006",
    "href": "cruise_anoxic_depth/batched_reports/WS20006.html#cruise-ws20006",
    "title": "WS20006 Report",
    "section": "Cruise WS20006",
    "text": "Cruise WS20006\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    54             4726             23554            2.6917\n2    54             4726             23554            5.2346"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS20006.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS20006.html#anoxic-depth-map",
    "title": "WS20006 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS20279.html",
    "href": "cruise_anoxic_depth/batched_reports/WS20279.html",
    "title": "WS20279 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS20279.html#cruise-ws20279",
    "href": "cruise_anoxic_depth/batched_reports/WS20279.html#cruise-ws20279",
    "title": "WS20279 Report",
    "section": "Cruise WS20279",
    "text": "Cruise WS20279\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    64             4727             23902            2.5146\n2    64             4727             23902            4.9411"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS20279.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS20279.html#anoxic-depth-map",
    "title": "WS20279 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS21032.html",
    "href": "cruise_anoxic_depth/batched_reports/WS21032.html",
    "title": "WS21032 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS21032.html#cruise-ws21032",
    "href": "cruise_anoxic_depth/batched_reports/WS21032.html#cruise-ws21032",
    "title": "WS21032 Report",
    "section": "Cruise WS21032",
    "text": "Cruise WS21032\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    68             4766             22432            2.7498\n2    68             4766             22432            5.4558"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS21032.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS21032.html#anoxic-depth-map",
    "title": "WS21032 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS21151.html",
    "href": "cruise_anoxic_depth/batched_reports/WS21151.html",
    "title": "WS21151 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS21151.html#cruise-ws21151",
    "href": "cruise_anoxic_depth/batched_reports/WS21151.html#cruise-ws21151",
    "title": "WS21151 Report",
    "section": "Cruise WS21151",
    "text": "Cruise WS21151\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    73             5228             25207            2.2067\n2    73             5228             25207            4.8422"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS21151.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS21151.html#anoxic-depth-map",
    "title": "WS21151 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS21278.html",
    "href": "cruise_anoxic_depth/batched_reports/WS21278.html",
    "title": "WS21278 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS21278.html#cruise-ws21278",
    "href": "cruise_anoxic_depth/batched_reports/WS21278.html#cruise-ws21278",
    "title": "WS21278 Report",
    "section": "Cruise WS21278",
    "text": "Cruise WS21278\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    64             4508             23366            0.0640\n2    64             4508             23366            5.9163"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS21278.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS21278.html#anoxic-depth-map",
    "title": "WS21278 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS22022.html",
    "href": "cruise_anoxic_depth/batched_reports/WS22022.html",
    "title": "WS22022 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS22022.html#cruise-ws22022",
    "href": "cruise_anoxic_depth/batched_reports/WS22022.html#cruise-ws22022",
    "title": "WS22022 Report",
    "section": "Cruise WS22022",
    "text": "Cruise WS22022\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    61             4415             25523            3.1962\n2    61             4415             25523            5.1376"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS22022.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS22022.html#anoxic-depth-map",
    "title": "WS22022 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS22141.html",
    "href": "cruise_anoxic_depth/batched_reports/WS22141.html",
    "title": "WS22141 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS22141.html#cruise-ws22141",
    "href": "cruise_anoxic_depth/batched_reports/WS22141.html#cruise-ws22141",
    "title": "WS22141 Report",
    "section": "Cruise WS22141",
    "text": "Cruise WS22141\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    66             4884             25127            2.7484\n2    66             4884             25127            4.9152"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS22141.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS22141.html#anoxic-depth-map",
    "title": "WS22141 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS22337.html",
    "href": "cruise_anoxic_depth/batched_reports/WS22337.html",
    "title": "WS22337 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS22337.html#cruise-ws22337",
    "href": "cruise_anoxic_depth/batched_reports/WS22337.html#cruise-ws22337",
    "title": "WS22337 Report",
    "section": "Cruise WS22337",
    "text": "Cruise WS22337\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    79             6208             24499            2.5375\n2    79             6208             24499            6.0611"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS22337.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS22337.html#anoxic-depth-map",
    "title": "WS22337 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS23061.html",
    "href": "cruise_anoxic_depth/batched_reports/WS23061.html",
    "title": "WS23061 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS23061.html#cruise-ws23061",
    "href": "cruise_anoxic_depth/batched_reports/WS23061.html#cruise-ws23061",
    "title": "WS23061 Report",
    "section": "Cruise WS23061",
    "text": "Cruise WS23061\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    86             6831             22386            2.6248\n2    86             6831             22386            5.1036"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS23061.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS23061.html#anoxic-depth-map",
    "title": "WS23061 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS24139.html",
    "href": "cruise_anoxic_depth/batched_reports/WS24139.html",
    "title": "WS24139 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(glue(\"{params$batch_name}/anoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_anoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS24139.html#cruise-ws24139",
    "href": "cruise_anoxic_depth/batched_reports/WS24139.html#cruise-ws24139",
    "title": "WS24139 Report",
    "section": "Cruise WS24139",
    "text": "Cruise WS24139\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (anoxic/hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    86             7124             22680            3.1716\n2    86             7124             22680            5.6171"
  },
  {
    "objectID": "cruise_anoxic_depth/batched_reports/WS24139.html#anoxic-depth-map",
    "href": "cruise_anoxic_depth/batched_reports/WS24139.html#anoxic-depth-map",
    "title": "WS24139 Report",
    "section": "Anoxic depth map",
    "text": "Anoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. The color bar uses a fixed depth scale so colors are comparable across thresholds.\n\n\nInteractive map of shallowest anoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_anoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  }
]