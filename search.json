[
  {
    "objectID": "cruise_hypoxic_depth/listing.html",
    "href": "cruise_hypoxic_depth/listing.html",
    "title": "cruise_hypoxic_depth",
    "section": "",
    "text": "Click on any element for more details.\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nSV18067 Report\n\n\n\n\n\n\n\n\n\n\n\n\nSV18173 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWB22215 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0603 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0612 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0618 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0623 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0704 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0718 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0802 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0807 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0901 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0906 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0914 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0919 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0923 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1004 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1007 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1015 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1018 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1102 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1106 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1113 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1116 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1202 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1207 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1212 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS14335 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS15103 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS15148 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS15208 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS15264 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS15320 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS16004 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS16074 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS16207 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS16263 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS16319 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS17030 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS17086 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS17170 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS17212 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS17282 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS18008 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS18120 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS18218 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS18285 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS18351 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS19028 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS19119 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS19210 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS19266 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS19322 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS20006 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS20231 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS20279 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS20342 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS21032 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS21093 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS21151 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS21212 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS21278 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS21338 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS22022 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS22072 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS22141 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS22281 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS22337 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS23011 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS23061 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS23203 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS24139 Report\n\n\n\n\n\nNo matching items"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS23203.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS23203.html",
    "title": "WS23203 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS23203.html#cruise-ws23203",
    "href": "cruise_hypoxic_depth/batched_reports/WS23203.html#cruise-ws23203",
    "title": "WS23203 Report",
    "section": "Cruise WS23203",
    "text": "Cruise WS23203\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    84             7037             23665            1.3486\n2    84             7037             23665            5.9850"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS23203.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS23203.html#hypoxic-depth-map",
    "title": "WS23203 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS23011.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS23011.html",
    "title": "WS23011 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS23011.html#cruise-ws23011",
    "href": "cruise_hypoxic_depth/batched_reports/WS23011.html#cruise-ws23011",
    "title": "WS23011 Report",
    "section": "Cruise WS23011",
    "text": "Cruise WS23011\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    72             4557             25992            3.2065\n2    72             4557             25992            5.6046"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS23011.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS23011.html#hypoxic-depth-map",
    "title": "WS23011 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS22281.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS22281.html",
    "title": "WS22281 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS22281.html#cruise-ws22281",
    "href": "cruise_hypoxic_depth/batched_reports/WS22281.html#cruise-ws22281",
    "title": "WS22281 Report",
    "section": "Cruise WS22281",
    "text": "Cruise WS22281\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    72             5434             24968            2.7499\n2    72             5434             24968            4.8539"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS22281.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS22281.html#hypoxic-depth-map",
    "title": "WS22281 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS22072.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS22072.html",
    "title": "WS22072 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS22072.html#cruise-ws22072",
    "href": "cruise_hypoxic_depth/batched_reports/WS22072.html#cruise-ws22072",
    "title": "WS22072 Report",
    "section": "Cruise WS22072",
    "text": "Cruise WS22072\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    75             6458             23007            2.6545\n2    75             6458             23007            5.1897"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS22072.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS22072.html#hypoxic-depth-map",
    "title": "WS22072 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS21338.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS21338.html",
    "title": "WS21338 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS21338.html#cruise-ws21338",
    "href": "cruise_hypoxic_depth/batched_reports/WS21338.html#cruise-ws21338",
    "title": "WS21338 Report",
    "section": "Cruise WS21338",
    "text": "Cruise WS21338\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    71             5697             25855            2.4974\n2    71             5697             25855            5.5520"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS21338.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS21338.html#hypoxic-depth-map",
    "title": "WS21338 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS21212.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS21212.html",
    "title": "WS21212 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS21212.html#cruise-ws21212",
    "href": "cruise_hypoxic_depth/batched_reports/WS21212.html#cruise-ws21212",
    "title": "WS21212 Report",
    "section": "Cruise WS21212",
    "text": "Cruise WS21212\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    69             4259             29494            1.4935\n2    69             4259             29494            4.5662"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS21212.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS21212.html#hypoxic-depth-map",
    "title": "WS21212 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS21093.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS21093.html",
    "title": "WS21093 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS21093.html#cruise-ws21093",
    "href": "cruise_hypoxic_depth/batched_reports/WS21093.html#cruise-ws21093",
    "title": "WS21093 Report",
    "section": "Cruise WS21093",
    "text": "Cruise WS21093\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    64             4075             52848            2.9809\n2    64             4075             52848            5.2020"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS21093.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS21093.html#hypoxic-depth-map",
    "title": "WS21093 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS20342.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS20342.html",
    "title": "WS20342 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS20342.html#cruise-ws20342",
    "href": "cruise_hypoxic_depth/batched_reports/WS20342.html#cruise-ws20342",
    "title": "WS20342 Report",
    "section": "Cruise WS20342",
    "text": "Cruise WS20342\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    67             4489             24573            2.7412\n2    67             4489             24573            5.1600"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS20342.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS20342.html#hypoxic-depth-map",
    "title": "WS20342 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS20231.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS20231.html",
    "title": "WS20231 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS20231.html#cruise-ws20231",
    "href": "cruise_hypoxic_depth/batched_reports/WS20231.html#cruise-ws20231",
    "title": "WS20231 Report",
    "section": "Cruise WS20231",
    "text": "Cruise WS20231\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    45             3034             30048            2.5097\n2    45             3034             30048            4.6257"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS20231.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS20231.html#hypoxic-depth-map",
    "title": "WS20231 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS19322.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS19322.html",
    "title": "WS19322 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS19322.html#cruise-ws19322",
    "href": "cruise_hypoxic_depth/batched_reports/WS19322.html#cruise-ws19322",
    "title": "WS19322 Report",
    "section": "Cruise WS19322",
    "text": "Cruise WS19322\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    68             4326             24319            3.2941\n2    68             4326             24319            5.8361"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS19322.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS19322.html#hypoxic-depth-map",
    "title": "WS19322 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS19210.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS19210.html",
    "title": "WS19210 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS19210.html#cruise-ws19210",
    "href": "cruise_hypoxic_depth/batched_reports/WS19210.html#cruise-ws19210",
    "title": "WS19210 Report",
    "section": "Cruise WS19210",
    "text": "Cruise WS19210\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    43             2488             29445            2.5009\n2    43             2488             29445            5.1254"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS19210.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS19210.html#hypoxic-depth-map",
    "title": "WS19210 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS19028.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS19028.html",
    "title": "WS19028 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS19028.html#cruise-ws19028",
    "href": "cruise_hypoxic_depth/batched_reports/WS19028.html#cruise-ws19028",
    "title": "WS19028 Report",
    "section": "Cruise WS19028",
    "text": "Cruise WS19028\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    32             1329             62029            3.6554\n2    32             1329             62029            5.7670"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS19028.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS19028.html#hypoxic-depth-map",
    "title": "WS19028 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS18285.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS18285.html",
    "title": "WS18285 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS18285.html#cruise-ws18285",
    "href": "cruise_hypoxic_depth/batched_reports/WS18285.html#cruise-ws18285",
    "title": "WS18285 Report",
    "section": "Cruise WS18285",
    "text": "Cruise WS18285\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    64             4193             24151            1.1090\n2    64             4193             24151            6.4699"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS18285.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS18285.html#hypoxic-depth-map",
    "title": "WS18285 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS18120.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS18120.html",
    "title": "WS18120 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS18120.html#cruise-ws18120",
    "href": "cruise_hypoxic_depth/batched_reports/WS18120.html#cruise-ws18120",
    "title": "WS18120 Report",
    "section": "Cruise WS18120",
    "text": "Cruise WS18120\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    20              924             60286            2.6945\n2    20              924             60286            4.9503"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS18120.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS18120.html#hypoxic-depth-map",
    "title": "WS18120 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS17282.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS17282.html",
    "title": "WS17282 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS17282.html#cruise-ws17282",
    "href": "cruise_hypoxic_depth/batched_reports/WS17282.html#cruise-ws17282",
    "title": "WS17282 Report",
    "section": "Cruise WS17282",
    "text": "Cruise WS17282\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    63             4772             48832            2.7664\n2    63             4772             48832            5.0644"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS17282.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS17282.html#hypoxic-depth-map",
    "title": "WS17282 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS17170.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS17170.html",
    "title": "WS17170 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS17170.html#cruise-ws17170",
    "href": "cruise_hypoxic_depth/batched_reports/WS17170.html#cruise-ws17170",
    "title": "WS17170 Report",
    "section": "Cruise WS17170",
    "text": "Cruise WS17170\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    27              507             89909            2.7159\n2    27              507             89909            4.4615"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS17170.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS17170.html#hypoxic-depth-map",
    "title": "WS17170 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS17030.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS17030.html",
    "title": "WS17030 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS17030.html#cruise-ws17030",
    "href": "cruise_hypoxic_depth/batched_reports/WS17030.html#cruise-ws17030",
    "title": "WS17030 Report",
    "section": "Cruise WS17030",
    "text": "Cruise WS17030\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    29             1803             42730            2.8508\n2    29             1803             42730            5.6084"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS17030.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS17030.html#hypoxic-depth-map",
    "title": "WS17030 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS16263.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS16263.html",
    "title": "WS16263 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS16263.html#cruise-ws16263",
    "href": "cruise_hypoxic_depth/batched_reports/WS16263.html#cruise-ws16263",
    "title": "WS16263 Report",
    "section": "Cruise WS16263",
    "text": "Cruise WS16263\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    28             1630             42251            2.0717\n2    28             1630             42251            4.9545"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS16263.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS16263.html#hypoxic-depth-map",
    "title": "WS16263 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS16074.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS16074.html",
    "title": "WS16074 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS16074.html#cruise-ws16074",
    "href": "cruise_hypoxic_depth/batched_reports/WS16074.html#cruise-ws16074",
    "title": "WS16074 Report",
    "section": "Cruise WS16074",
    "text": "Cruise WS16074\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    31             1152             72889            2.5237\n2    31             1152             72889            5.5489"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS16074.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS16074.html#hypoxic-depth-map",
    "title": "WS16074 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS15320.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS15320.html",
    "title": "WS15320 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS15320.html#cruise-ws15320",
    "href": "cruise_hypoxic_depth/batched_reports/WS15320.html#cruise-ws15320",
    "title": "WS15320 Report",
    "section": "Cruise WS15320",
    "text": "Cruise WS15320\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    30             1411             44548            2.0856\n2    30             1411             44548            4.8811"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS15320.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS15320.html#hypoxic-depth-map",
    "title": "WS15320 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS15208.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS15208.html",
    "title": "WS15208 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS15208.html#cruise-ws15208",
    "href": "cruise_hypoxic_depth/batched_reports/WS15208.html#cruise-ws15208",
    "title": "WS15208 Report",
    "section": "Cruise WS15208",
    "text": "Cruise WS15208\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    45             7740             16079            1.4801\n2    45             7740             16079            5.5960"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS15208.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS15208.html#hypoxic-depth-map",
    "title": "WS15208 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS15103.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS15103.html",
    "title": "WS15103 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS15103.html#cruise-ws15103",
    "href": "cruise_hypoxic_depth/batched_reports/WS15103.html#cruise-ws15103",
    "title": "WS15103 Report",
    "section": "Cruise WS15103",
    "text": "Cruise WS15103\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    31             1721             45626            1.0065\n2    31             1721             45626            6.3063"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS15103.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS15103.html#hypoxic-depth-map",
    "title": "WS15103 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1212.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS1212.html",
    "title": "WS1212 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1212.html#cruise-ws1212",
    "href": "cruise_hypoxic_depth/batched_reports/WS1212.html#cruise-ws1212",
    "title": "WS1212 Report",
    "section": "Cruise WS1212",
    "text": "Cruise WS1212\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    35             4956             22697            2.6969\n2    35             4956             22697            4.7057"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1212.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS1212.html#hypoxic-depth-map",
    "title": "WS1212 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1202.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS1202.html",
    "title": "WS1202 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1202.html#cruise-ws1202",
    "href": "cruise_hypoxic_depth/batched_reports/WS1202.html#cruise-ws1202",
    "title": "WS1202 Report",
    "section": "Cruise WS1202",
    "text": "Cruise WS1202\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    33             4863             25041            1.7207\n2    33             4863             25041            4.8915"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1202.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS1202.html#hypoxic-depth-map",
    "title": "WS1202 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1113.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS1113.html",
    "title": "WS1113 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1113.html#cruise-ws1113",
    "href": "cruise_hypoxic_depth/batched_reports/WS1113.html#cruise-ws1113",
    "title": "WS1113 Report",
    "section": "Cruise WS1113",
    "text": "Cruise WS1113\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    39            10058             41965            1.1699\n2    39            10058             41965            3.9663"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1113.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS1113.html#hypoxic-depth-map",
    "title": "WS1113 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1102.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS1102.html",
    "title": "WS1102 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1102.html#cruise-ws1102",
    "href": "cruise_hypoxic_depth/batched_reports/WS1102.html#cruise-ws1102",
    "title": "WS1102 Report",
    "section": "Cruise WS1102",
    "text": "Cruise WS1102\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    33             5968             22640            1.7263\n2    33             5968             22640            3.5704"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1102.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS1102.html#hypoxic-depth-map",
    "title": "WS1102 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1015.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS1015.html",
    "title": "WS1015 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1015.html#cruise-ws1015",
    "href": "cruise_hypoxic_depth/batched_reports/WS1015.html#cruise-ws1015",
    "title": "WS1015 Report",
    "section": "Cruise WS1015",
    "text": "Cruise WS1015\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    48             4751             18047            0.6045\n2    48             4751             18047            0.6065"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1015.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS1015.html#hypoxic-depth-map",
    "title": "WS1015 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1004.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS1004.html",
    "title": "WS1004 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1004.html#cruise-ws1004",
    "href": "cruise_hypoxic_depth/batched_reports/WS1004.html#cruise-ws1004",
    "title": "WS1004 Report",
    "section": "Cruise WS1004",
    "text": "Cruise WS1004\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    31             3027             34386            2.7320\n2    31             3027             34386            5.6318"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1004.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS1004.html#hypoxic-depth-map",
    "title": "WS1004 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0919.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS0919.html",
    "title": "WS0919 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0919.html#cruise-ws0919",
    "href": "cruise_hypoxic_depth/batched_reports/WS0919.html#cruise-ws0919",
    "title": "WS0919 Report",
    "section": "Cruise WS0919",
    "text": "Cruise WS0919\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    25             3036             29357            2.6526\n2    25             3036             29357            4.5363"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0919.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS0919.html#hypoxic-depth-map",
    "title": "WS0919 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0906.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS0906.html",
    "title": "WS0906 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0906.html#cruise-ws0906",
    "href": "cruise_hypoxic_depth/batched_reports/WS0906.html#cruise-ws0906",
    "title": "WS0906 Report",
    "section": "Cruise WS0906",
    "text": "Cruise WS0906\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    26             3737             28176            2.1732\n2    26             3737             28176            4.2541"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0906.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS0906.html#hypoxic-depth-map",
    "title": "WS0906 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0807.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS0807.html",
    "title": "WS0807 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0807.html#cruise-ws0807",
    "href": "cruise_hypoxic_depth/batched_reports/WS0807.html#cruise-ws0807",
    "title": "WS0807 Report",
    "section": "Cruise WS0807",
    "text": "Cruise WS0807\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    37             5480             15685            2.4070\n2    37             5480             15685            5.1997"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0807.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS0807.html#hypoxic-depth-map",
    "title": "WS0807 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0718.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS0718.html",
    "title": "WS0718 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0718.html#cruise-ws0718",
    "href": "cruise_hypoxic_depth/batched_reports/WS0718.html#cruise-ws0718",
    "title": "WS0718 Report",
    "section": "Cruise WS0718",
    "text": "Cruise WS0718\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    38             5167             17386            2.6713\n2    38             5167             17386            5.1821"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0718.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS0718.html#hypoxic-depth-map",
    "title": "WS0718 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0623.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS0623.html",
    "title": "WS0623 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0623.html#cruise-ws0623",
    "href": "cruise_hypoxic_depth/batched_reports/WS0623.html#cruise-ws0623",
    "title": "WS0623 Report",
    "section": "Cruise WS0623",
    "text": "Cruise WS0623\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    33             1731             44718            1.4524\n2    33             1731             44718            3.7931"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0623.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS0623.html#hypoxic-depth-map",
    "title": "WS0623 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0612.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS0612.html",
    "title": "WS0612 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0612.html#cruise-ws0612",
    "href": "cruise_hypoxic_depth/batched_reports/WS0612.html#cruise-ws0612",
    "title": "WS0612 Report",
    "section": "Cruise WS0612",
    "text": "Cruise WS0612\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    89            14423             11876            1.8646\n2    89            14423             11876            5.5601"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0612.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS0612.html#hypoxic-depth-map",
    "title": "WS0612 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WB22215.html",
    "href": "cruise_hypoxic_depth/batched_reports/WB22215.html",
    "title": "WB22215 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WB22215.html#cruise-wb22215",
    "href": "cruise_hypoxic_depth/batched_reports/WB22215.html#cruise-wb22215",
    "title": "WB22215 Report",
    "section": "Cruise WB22215",
    "text": "Cruise WB22215\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    58             5093             27205            1.8209\n2    58             5093             27205            5.1271"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WB22215.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WB22215.html#hypoxic-depth-map",
    "title": "WB22215 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/SV18067.html",
    "href": "cruise_hypoxic_depth/batched_reports/SV18067.html",
    "title": "SV18067 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/SV18067.html#cruise-sv18067",
    "href": "cruise_hypoxic_depth/batched_reports/SV18067.html#cruise-sv18067",
    "title": "SV18067 Report",
    "section": "Cruise SV18067",
    "text": "Cruise SV18067\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    28             2624             42376            3.0303\n2    28             2624             42376            5.3231"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/SV18067.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/SV18067.html#hypoxic-depth-map",
    "title": "SV18067 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS24139.html",
    "href": "cruise_oxygen/batched_reports/WS24139.html",
    "title": "WS24139 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS24139.html#cruise-ws24139",
    "href": "cruise_oxygen/batched_reports/WS24139.html#cruise-ws24139",
    "title": "WS24139 Report",
    "section": "Cruise WS24139",
    "text": "Cruise WS24139\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    87             7118             22680            3.1716\n2    87             7118             22680            5.6171"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS24139.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS24139.html#interpolated-field-map",
    "title": "WS24139 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS23061.html",
    "href": "cruise_oxygen/batched_reports/WS23061.html",
    "title": "WS23061 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS23061.html#cruise-ws23061",
    "href": "cruise_oxygen/batched_reports/WS23061.html#cruise-ws23061",
    "title": "WS23061 Report",
    "section": "Cruise WS23061",
    "text": "Cruise WS23061\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    86             6802             22404            2.6248\n2    86             6802             22404            5.1036"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS23061.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS23061.html#interpolated-field-map",
    "title": "WS23061 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22337.html",
    "href": "cruise_oxygen/batched_reports/WS22337.html",
    "title": "WS22337 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22337.html#cruise-ws22337",
    "href": "cruise_oxygen/batched_reports/WS22337.html#cruise-ws22337",
    "title": "WS22337 Report",
    "section": "Cruise WS22337",
    "text": "Cruise WS22337\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    79             6166             24499            2.5375\n2    79             6166             24499            6.0611"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22337.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS22337.html#interpolated-field-map",
    "title": "WS22337 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22141.html",
    "href": "cruise_oxygen/batched_reports/WS22141.html",
    "title": "WS22141 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22141.html#cruise-ws22141",
    "href": "cruise_oxygen/batched_reports/WS22141.html#cruise-ws22141",
    "title": "WS22141 Report",
    "section": "Cruise WS22141",
    "text": "Cruise WS22141\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    65             4834             25124            2.7484\n2    65             4834             25124            4.9152"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22141.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS22141.html#interpolated-field-map",
    "title": "WS22141 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22022.html",
    "href": "cruise_oxygen/batched_reports/WS22022.html",
    "title": "WS22022 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22022.html#cruise-ws22022",
    "href": "cruise_oxygen/batched_reports/WS22022.html#cruise-ws22022",
    "title": "WS22022 Report",
    "section": "Cruise WS22022",
    "text": "Cruise WS22022\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    61             4415             25523            3.1962\n2    61             4415             25523            5.1376"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22022.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS22022.html#interpolated-field-map",
    "title": "WS22022 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21278.html",
    "href": "cruise_oxygen/batched_reports/WS21278.html",
    "title": "WS21278 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21278.html#cruise-ws21278",
    "href": "cruise_oxygen/batched_reports/WS21278.html#cruise-ws21278",
    "title": "WS21278 Report",
    "section": "Cruise WS21278",
    "text": "Cruise WS21278\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    66             4463             23368            1.0401\n2    66             4463             23368            5.9163"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21278.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS21278.html#interpolated-field-map",
    "title": "WS21278 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21151.html",
    "href": "cruise_oxygen/batched_reports/WS21151.html",
    "title": "WS21151 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21151.html#cruise-ws21151",
    "href": "cruise_oxygen/batched_reports/WS21151.html#cruise-ws21151",
    "title": "WS21151 Report",
    "section": "Cruise WS21151",
    "text": "Cruise WS21151\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    74             5166             25207            2.2067\n2    74             5166             25207            5.1807"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21151.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS21151.html#interpolated-field-map",
    "title": "WS21151 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21032.html",
    "href": "cruise_oxygen/batched_reports/WS21032.html",
    "title": "WS21032 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21032.html#cruise-ws21032",
    "href": "cruise_oxygen/batched_reports/WS21032.html#cruise-ws21032",
    "title": "WS21032 Report",
    "section": "Cruise WS21032",
    "text": "Cruise WS21032\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    68             4777             22432            2.7498\n2    68             4777             22432            5.4938"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21032.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS21032.html#interpolated-field-map",
    "title": "WS21032 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS20279.html",
    "href": "cruise_oxygen/batched_reports/WS20279.html",
    "title": "WS20279 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS20279.html#cruise-ws20279",
    "href": "cruise_oxygen/batched_reports/WS20279.html#cruise-ws20279",
    "title": "WS20279 Report",
    "section": "Cruise WS20279",
    "text": "Cruise WS20279\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    64             4677             23902            2.5146\n2    64             4677             23902            4.9411"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS20279.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS20279.html#interpolated-field-map",
    "title": "WS20279 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS20006.html",
    "href": "cruise_oxygen/batched_reports/WS20006.html",
    "title": "WS20006 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS20006.html#cruise-ws20006",
    "href": "cruise_oxygen/batched_reports/WS20006.html#cruise-ws20006",
    "title": "WS20006 Report",
    "section": "Cruise WS20006",
    "text": "Cruise WS20006\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    54             4737             23604            2.6919\n2    54             4737             23604            5.2266"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS20006.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS20006.html#interpolated-field-map",
    "title": "WS20006 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19266.html",
    "href": "cruise_oxygen/batched_reports/WS19266.html",
    "title": "WS19266 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19266.html#cruise-ws19266",
    "href": "cruise_oxygen/batched_reports/WS19266.html#cruise-ws19266",
    "title": "WS19266 Report",
    "section": "Cruise WS19266",
    "text": "Cruise WS19266\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    64             5288             22003            2.7045\n2    64             5288             22003            5.3170"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19266.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS19266.html#interpolated-field-map",
    "title": "WS19266 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19119.html",
    "href": "cruise_oxygen/batched_reports/WS19119.html",
    "title": "WS19119 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19119.html#cruise-ws19119",
    "href": "cruise_oxygen/batched_reports/WS19119.html#cruise-ws19119",
    "title": "WS19119 Report",
    "section": "Cruise WS19119",
    "text": "Cruise WS19119\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    44             2402             29505            2.8493\n2    44             2402             29505            5.0629"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19119.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS19119.html#interpolated-field-map",
    "title": "WS19119 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18351.html",
    "href": "cruise_oxygen/batched_reports/WS18351.html",
    "title": "WS18351 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18351.html#cruise-ws18351",
    "href": "cruise_oxygen/batched_reports/WS18351.html#cruise-ws18351",
    "title": "WS18351 Report",
    "section": "Cruise WS18351",
    "text": "Cruise WS18351\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    12             1556             54512            3.1524\n2    12             1556             54512            4.8139"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18351.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS18351.html#interpolated-field-map",
    "title": "WS18351 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18218.html",
    "href": "cruise_oxygen/batched_reports/WS18218.html",
    "title": "WS18218 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18218.html#cruise-ws18218",
    "href": "cruise_oxygen/batched_reports/WS18218.html#cruise-ws18218",
    "title": "WS18218 Report",
    "section": "Cruise WS18218",
    "text": "Cruise WS18218\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    25             1789             45670            2.1798\n2    25             1789             45670            4.8945"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18218.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS18218.html#interpolated-field-map",
    "title": "WS18218 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18008.html",
    "href": "cruise_oxygen/batched_reports/WS18008.html",
    "title": "WS18008 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18008.html#cruise-ws18008",
    "href": "cruise_oxygen/batched_reports/WS18008.html#cruise-ws18008",
    "title": "WS18008 Report",
    "section": "Cruise WS18008",
    "text": "Cruise WS18008\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    33             1842             36649            3.7695\n2    33             1842             36649            5.9260"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18008.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS18008.html#interpolated-field-map",
    "title": "WS18008 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17212.html",
    "href": "cruise_oxygen/batched_reports/WS17212.html",
    "title": "WS17212 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17212.html#cruise-ws17212",
    "href": "cruise_oxygen/batched_reports/WS17212.html#cruise-ws17212",
    "title": "WS17212 Report",
    "section": "Cruise WS17212",
    "text": "Cruise WS17212\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    29             1879             43531            1.8337\n2    29             1879             43531            3.5262"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17212.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS17212.html#interpolated-field-map",
    "title": "WS17212 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17086.html",
    "href": "cruise_oxygen/batched_reports/WS17086.html",
    "title": "WS17086 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17086.html#cruise-ws17086",
    "href": "cruise_oxygen/batched_reports/WS17086.html#cruise-ws17086",
    "title": "WS17086 Report",
    "section": "Cruise WS17086",
    "text": "Cruise WS17086\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    32             2015             42740            3.0186\n2    32             2015             42740            5.2248"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17086.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS17086.html#interpolated-field-map",
    "title": "WS17086 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16319.html",
    "href": "cruise_oxygen/batched_reports/WS16319.html",
    "title": "WS16319 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16319.html#cruise-ws16319",
    "href": "cruise_oxygen/batched_reports/WS16319.html#cruise-ws16319",
    "title": "WS16319 Report",
    "section": "Cruise WS16319",
    "text": "Cruise WS16319\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    36             1754             45295            3.0559\n2    36             1754             45295            5.0266"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16319.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS16319.html#interpolated-field-map",
    "title": "WS16319 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16207.html",
    "href": "cruise_oxygen/batched_reports/WS16207.html",
    "title": "WS16207 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16207.html#cruise-ws16207",
    "href": "cruise_oxygen/batched_reports/WS16207.html#cruise-ws16207",
    "title": "WS16207 Report",
    "section": "Cruise WS16207",
    "text": "Cruise WS16207\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    30             1680             43754            2.6401\n2    30             1680             43754            5.1674"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16207.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS16207.html#interpolated-field-map",
    "title": "WS16207 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16004.html",
    "href": "cruise_oxygen/batched_reports/WS16004.html",
    "title": "WS16004 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16004.html#cruise-ws16004",
    "href": "cruise_oxygen/batched_reports/WS16004.html#cruise-ws16004",
    "title": "WS16004 Report",
    "section": "Cruise WS16004",
    "text": "Cruise WS16004\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    32             1784             40849             2.709\n2    32             1784             40849             4.459"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16004.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS16004.html#interpolated-field-map",
    "title": "WS16004 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15264.html",
    "href": "cruise_oxygen/batched_reports/WS15264.html",
    "title": "WS15264 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15264.html#cruise-ws15264",
    "href": "cruise_oxygen/batched_reports/WS15264.html#cruise-ws15264",
    "title": "WS15264 Report",
    "section": "Cruise WS15264",
    "text": "Cruise WS15264\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    35             1894             32465            2.2688\n2    35             1894             32465            4.7455"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15264.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS15264.html#interpolated-field-map",
    "title": "WS15264 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15148.html",
    "href": "cruise_oxygen/batched_reports/WS15148.html",
    "title": "WS15148 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15148.html#cruise-ws15148",
    "href": "cruise_oxygen/batched_reports/WS15148.html#cruise-ws15148",
    "title": "WS15148 Report",
    "section": "Cruise WS15148",
    "text": "Cruise WS15148\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    32             1682             47579            1.9652\n2    32             1682             47579            4.8437"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15148.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS15148.html#interpolated-field-map",
    "title": "WS15148 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS14335.html",
    "href": "cruise_oxygen/batched_reports/WS14335.html",
    "title": "WS14335 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS14335.html#cruise-ws14335",
    "href": "cruise_oxygen/batched_reports/WS14335.html#cruise-ws14335",
    "title": "WS14335 Report",
    "section": "Cruise WS14335",
    "text": "Cruise WS14335\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    35              593             80084            3.4859\n2    35              593             80084            4.9883"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS14335.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS14335.html#interpolated-field-map",
    "title": "WS14335 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1207.html",
    "href": "cruise_oxygen/batched_reports/WS1207.html",
    "title": "WS1207 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1207.html#cruise-ws1207",
    "href": "cruise_oxygen/batched_reports/WS1207.html#cruise-ws1207",
    "title": "WS1207 Report",
    "section": "Cruise WS1207",
    "text": "Cruise WS1207\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    36             4847             25511            2.1925\n2    36             4847             25511            4.1931"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1207.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS1207.html#interpolated-field-map",
    "title": "WS1207 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1116.html",
    "href": "cruise_oxygen/batched_reports/WS1116.html",
    "title": "WS1116 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1116.html#cruise-ws1116",
    "href": "cruise_oxygen/batched_reports/WS1116.html#cruise-ws1116",
    "title": "WS1116 Report",
    "section": "Cruise WS1116",
    "text": "Cruise WS1116\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    24             5119             24591            2.7303\n2    24             5119             24591            4.6591"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1116.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS1116.html#interpolated-field-map",
    "title": "WS1116 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1106.html",
    "href": "cruise_oxygen/batched_reports/WS1106.html",
    "title": "WS1106 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1106.html#cruise-ws1106",
    "href": "cruise_oxygen/batched_reports/WS1106.html#cruise-ws1106",
    "title": "WS1106 Report",
    "section": "Cruise WS1106",
    "text": "Cruise WS1106\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    30             4397             22864            2.1854\n2    30             4397             22864            4.2590"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1106.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS1106.html#interpolated-field-map",
    "title": "WS1106 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1018.html",
    "href": "cruise_oxygen/batched_reports/WS1018.html",
    "title": "WS1018 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1018.html#cruise-ws1018",
    "href": "cruise_oxygen/batched_reports/WS1018.html#cruise-ws1018",
    "title": "WS1018 Report",
    "section": "Cruise WS1018",
    "text": "Cruise WS1018\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    32             4131             27485            1.1045\n2    32             4131             27485            2.0343"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1018.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS1018.html#interpolated-field-map",
    "title": "WS1018 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1007.html",
    "href": "cruise_oxygen/batched_reports/WS1007.html",
    "title": "WS1007 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1007.html#cruise-ws1007",
    "href": "cruise_oxygen/batched_reports/WS1007.html#cruise-ws1007",
    "title": "WS1007 Report",
    "section": "Cruise WS1007",
    "text": "Cruise WS1007\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    35             4134             24248            2.7489\n2    35             4134             24248            5.5538"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1007.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS1007.html#interpolated-field-map",
    "title": "WS1007 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0923.html",
    "href": "cruise_oxygen/batched_reports/WS0923.html",
    "title": "WS0923 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0923.html#cruise-ws0923",
    "href": "cruise_oxygen/batched_reports/WS0923.html#cruise-ws0923",
    "title": "WS0923 Report",
    "section": "Cruise WS0923",
    "text": "Cruise WS0923\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    38             4544             31340            2.7817\n2    38             4544             31340            5.2614"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0923.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS0923.html#interpolated-field-map",
    "title": "WS0923 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0914.html",
    "href": "cruise_oxygen/batched_reports/WS0914.html",
    "title": "WS0914 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0914.html#cruise-ws0914",
    "href": "cruise_oxygen/batched_reports/WS0914.html#cruise-ws0914",
    "title": "WS0914 Report",
    "section": "Cruise WS0914",
    "text": "Cruise WS0914\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    26             4671             24733            2.3144\n2    26             4671             24733            4.6715"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0914.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS0914.html#interpolated-field-map",
    "title": "WS0914 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0901.html",
    "href": "cruise_oxygen/batched_reports/WS0901.html",
    "title": "WS0901 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0901.html#cruise-ws0901",
    "href": "cruise_oxygen/batched_reports/WS0901.html#cruise-ws0901",
    "title": "WS0901 Report",
    "section": "Cruise WS0901",
    "text": "Cruise WS0901\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    39             5131             18835            2.6348\n2    39             5131             18835            5.5332"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0901.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS0901.html#interpolated-field-map",
    "title": "WS0901 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0802.html",
    "href": "cruise_oxygen/batched_reports/WS0802.html",
    "title": "WS0802 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0802.html#cruise-ws0802",
    "href": "cruise_oxygen/batched_reports/WS0802.html#cruise-ws0802",
    "title": "WS0802 Report",
    "section": "Cruise WS0802",
    "text": "Cruise WS0802\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    37             5394             17393            2.6808\n2    37             5394             17393            5.2205"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0802.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS0802.html#interpolated-field-map",
    "title": "WS0802 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0704.html",
    "href": "cruise_oxygen/batched_reports/WS0704.html",
    "title": "WS0704 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0704.html#cruise-ws0704",
    "href": "cruise_oxygen/batched_reports/WS0704.html#cruise-ws0704",
    "title": "WS0704 Report",
    "section": "Cruise WS0704",
    "text": "Cruise WS0704\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    36             4950             18160            1.8837\n2    36             4950             18160            4.0938"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0704.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS0704.html#interpolated-field-map",
    "title": "WS0704 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0618.html",
    "href": "cruise_oxygen/batched_reports/WS0618.html",
    "title": "WS0618 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0618.html#cruise-ws0618",
    "href": "cruise_oxygen/batched_reports/WS0618.html#cruise-ws0618",
    "title": "WS0618 Report",
    "section": "Cruise WS0618",
    "text": "Cruise WS0618\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    49             9319             12347            1.8812\n2    49             9319             12347            4.6943"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0618.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS0618.html#interpolated-field-map",
    "title": "WS0618 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0603.html",
    "href": "cruise_oxygen/batched_reports/WS0603.html",
    "title": "WS0603 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0603.html#cruise-ws0603",
    "href": "cruise_oxygen/batched_reports/WS0603.html#cruise-ws0603",
    "title": "WS0603 Report",
    "section": "Cruise WS0603",
    "text": "Cruise WS0603\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    94            13066             12087            1.8824\n2    94            13066             12087            4.0451"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0603.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS0603.html#interpolated-field-map",
    "title": "WS0603 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/SV18173.html",
    "href": "cruise_oxygen/batched_reports/SV18173.html",
    "title": "SV18173 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/SV18173.html#cruise-sv18173",
    "href": "cruise_oxygen/batched_reports/SV18173.html#cruise-sv18173",
    "title": "SV18173 Report",
    "section": "Cruise SV18173",
    "text": "Cruise SV18173\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    29             1938             45866            2.6239\n2    29             1938             45866            5.1915"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/SV18173.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/SV18173.html#interpolated-field-map",
    "title": "SV18173 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "reports/hypoxic_extent.html",
    "href": "reports/hypoxic_extent.html",
    "title": "Hypoxic extent across cruises",
    "section": "",
    "text": "This report summarizes when (by cruise date and depth from CTD casts) and where (from interpolated oxygen fields) dissolved oxygen falls below a hypoxic threshold. The default threshold is 2.0 mg/L. Change params.o2_threshold in the YAML front matter or pass --execute-params when rendering:\nRun after processing and interpolation:"
  },
  {
    "objectID": "reports/hypoxic_extent.html#overview",
    "href": "reports/hypoxic_extent.html#overview",
    "title": "Hypoxic extent across cruises",
    "section": "Overview",
    "text": "Overview\n\n\n\nMetric\nValue\n\n\n\n\nO₂ threshold (mg/L)\n2\n\n\nCruises with cleaned CTD data\n72\n\n\nCruises with interpolated fields\n72\n\n\nCruises with hypoxic casts\n19\n\n\nHypoxic casts\n82\n\n\nTotal CTD observations\n288030"
  },
  {
    "objectID": "reports/hypoxic_extent.html#when-hypoxic-extent-over-time",
    "href": "reports/hypoxic_extent.html#when-hypoxic-extent-over-time",
    "title": "Hypoxic extent across cruises",
    "section": "When: hypoxic extent over time",
    "text": "When: hypoxic extent over time\nThe timeline below tracks hypoxia in each cruise’s CTD casts. Surface hypoxic is the share of casts whose shallowest sample is below the threshold; any depth is the share of casts with at least one sample below the threshold. Median cast depth (right axis) is the median deepest sample depth per cruise.\n\n\n\n\n\nHypoxic cast coverage by cruise date.\n\n\n\n\nMedian shallow hypoxic depth summarizes how close to the surface hypoxia begins in each cruise (among casts that go hypoxic at any depth). Median cast depth (right axis) shows how deep casts typically sampled.\n\n\n\n\n\nMedian shallowest hypoxic depth among hypoxic casts, by cruise."
  },
  {
    "objectID": "reports/hypoxic_extent.html#when-calendar-heatmap-from-interpolated-fields",
    "href": "reports/hypoxic_extent.html#when-calendar-heatmap-from-interpolated-fields",
    "title": "Hypoxic extent across cruises",
    "section": "When: calendar heatmap from interpolated fields",
    "text": "When: calendar heatmap from interpolated fields\nEach tile is a cruise week (ISO year × week). Color shows the shallowest hypoxic depth (m) in the interpolated oxygen field— the minimum depth at which O₂ falls below the threshold (default 2.0 mg/L, set via params.o2_threshold). Shallow hypoxia is yellow; deeper hypoxia is purple. Weeks with no hypoxia are left blank.\n\n\n\n\n\nShallowest hypoxic depth by cruise week from interpolated fields."
  },
  {
    "objectID": "reports/hypoxic_extent.html#when-hypoxic-extent-by-depth",
    "href": "reports/hypoxic_extent.html#when-hypoxic-extent-by-depth",
    "title": "Hypoxic extent across cruises",
    "section": "When: hypoxic extent by depth",
    "text": "When: hypoxic extent by depth\nEach row is a cruise; color is the percentage of CTD observations below the threshold in each depth bin.\n\n\n\n\n\nPercent of CTD observations below the hypoxic threshold at each depth."
  },
  {
    "objectID": "reports/hypoxic_extent.html#where-3d-hypoxic-volume",
    "href": "reports/hypoxic_extent.html#where-3d-hypoxic-volume",
    "title": "Hypoxic extent across cruises",
    "section": "Where: 3D hypoxic volume",
    "text": "Where: 3D hypoxic volume\nInteractive 3D view of hypoxic regions from interpolated fields, with BlueTopo bathymetry as the seafloor surface and coastline basemap lines at the sea surface. Each voxel shows the share of cruises with O₂ below the threshold at that location and depth (aligned to a common 4 m depth grid). Hypoxic volumes render at 60% opacity; non-hypoxic space is transparent. The color bar uses a log scale (1–100%). Drag to rotate, scroll to zoom."
  },
  {
    "objectID": "reports/hypoxic_extent.html#where-hypoxia-frequency-from-interpolated-fields",
    "href": "reports/hypoxic_extent.html#where-hypoxia-frequency-from-interpolated-fields",
    "title": "Hypoxic extent across cruises",
    "section": "Where: hypoxia frequency from interpolated fields",
    "text": "Where: hypoxia frequency from interpolated fields\nUse Prev / Next to step through ecological depth layers (same bins as the oxygen field maps). Color shows the log-scaled percentage of cruises whose interpolated field is hypoxic (O₂ below the threshold) at each grid cell within that layer."
  },
  {
    "objectID": "reports/hypoxic_extent.html#cruise-summary-table",
    "href": "reports/hypoxic_extent.html#cruise-summary-table",
    "title": "Hypoxic extent across cruises",
    "section": "Cruise summary table",
    "text": "Cruise summary table\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nCruise\nDate\nCasts\nHypoxic casts\nObservations\n% shallow hypoxic\n% casts hypoxic\n% obs hypoxic\nMedian shallow hypoxic depth (m)\nMin O₂ (mg/L)\nMax O₂ (mg/L)\n\n\n\n\nWS0603\n2006-01-18\n92\n3\n11852\n0.0\n3.3\n13.3\n228.7\n1.88\n4.05\n\n\nWS0612\n2006-05-02\n89\n3\n12943\n1.1\n3.4\n14.3\n240.4\n1.86\n5.56\n\n\nWS0618\n2006-08-05\n49\n4\n8506\n0.0\n8.2\n19.8\n141.4\n1.88\n4.69\n\n\nWS0623\n2006-10-31\n33\n1\n1195\n3.0\n3.0\n1.3\n0.4\n1.45\n3.79\n\n\nWS0704\n2007-02-17\n35\n1\n4267\n0.0\n2.9\n11.6\n201.9\n1.88\n4.09\n\n\nWS0718\n2007-07-08\n36\n0\n4911\n0.0\n0.0\n0.0\nNA\n2.67\n5.18\n\n\nWS0802\n2008-02-08\n34\n0\n5080\n0.0\n0.0\n0.0\nNA\n2.68\n5.22\n\n\nWS0807\n2008-05-06\n37\n0\n5129\n0.0\n0.0\n0.0\nNA\n2.41\n5.20\n\n\nWS0901\n2009-01-21\n39\n0\n4668\n0.0\n0.0\n0.0\nNA\n2.63\n5.53\n\n\nWS0906\n2009-04-20\n25\n0\n3396\n0.0\n0.0\n0.0\nNA\n2.17\n4.25\n\n\nWS0914\n2009-08-24\n26\n0\n4626\n0.0\n0.0\n0.0\nNA\n2.31\n4.67\n\n\nWS0919\n2009-10-26\n25\n0\n2965\n0.0\n0.0\n0.0\nNA\n2.65\n4.54\n\n\nWS0923\n2009-12-13\n38\n0\n4489\n0.0\n0.0\n0.0\nNA\n2.78\n5.26\n\n\nWS1004\n2010-03-08\n31\n0\n3001\n0.0\n0.0\n0.0\nNA\n2.73\n5.63\n\n\nWS1007\n2010-05-03\n35\n0\n4078\n0.0\n0.0\n0.0\nNA\n2.75\n5.55\n\n\nWS1015\n2010-08-16\n1\n1\n5\n100.0\n100.0\n100.0\n30.0\n0.60\n0.61\n\n\nWS1018\n2010-10-12\n31\n30\n3686\n96.8\n96.8\n99.9\n2.6\n1.10\n2.03\n\n\nWS1102\n2011-02-21\n33\n5\n5821\n0.0\n15.2\n38.2\n70.1\n1.73\n3.57\n\n\nWS1106\n2011-04-04\n30\n0\n4332\n0.0\n0.0\n0.0\nNA\n2.19\n4.26\n\n\nWS1113\n2011-08-02\n38\n1\n9773\n2.6\n2.6\n0.5\n1.6\n1.17\n3.97\n\n\nWS1116\n2011-10-20\n24\n0\n5080\n0.0\n0.0\n0.0\nNA\n2.73\n4.66\n\n\nWS1202\n2012-02-27\n33\n9\n4819\n27.3\n27.3\n11.5\n2.9\n1.72\n4.89\n\n\nWS1207\n2012-06-04\n36\n0\n4821\n0.0\n0.0\n0.0\nNA\n2.19\n4.19\n\n\nWS1212\n2012-08-06\n35\n0\n4896\n0.0\n0.0\n0.0\nNA\n2.70\n4.71\n\n\nWS14335\n2014-12-01\n34\n0\n535\n0.0\n0.0\n0.0\nNA\n3.49\n4.99\n\n\nWS15103\n2015-04-13\n27\n4\n1515\n3.7\n14.8\n1.6\n18.5\n1.01\n6.31\n\n\nWS15148\n2015-06-01\n31\n1\n1637\n3.2\n3.2\n0.1\n1.4\n1.97\n4.84\n\n\nWS15208\n2015-07-27\n43\n2\n7628\n0.0\n4.7\n0.2\n53.0\n1.48\n5.60\n\n\nWS15264\n2015-09-21\n34\n0\n1841\n0.0\n0.0\n0.0\nNA\n2.27\n4.75\n\n\nWS15320\n2015-11-16\n30\n0\n1383\n0.0\n0.0\n0.0\nNA\n2.09\n4.88\n\n\nWS16004\n2016-01-04\n32\n0\n1731\n0.0\n0.0\n0.0\nNA\n2.71\n4.46\n\n\nWS16074\n2016-03-14\n31\n0\n1112\n0.0\n0.0\n0.0\nNA\n2.52\n5.55\n\n\nWS16207\n2016-07-25\n30\n0\n1612\n0.0\n0.0\n0.0\nNA\n2.64\n5.17\n\n\nWS16263\n2016-09-19\n28\n0\n1581\n0.0\n0.0\n0.0\nNA\n2.07\n4.95\n\n\nWS16319\n2016-11-14\n35\n0\n1705\n0.0\n0.0\n0.0\nNA\n3.06\n5.03\n\n\nWS17030\n2017-01-30\n29\n0\n1765\n0.0\n0.0\n0.0\nNA\n2.85\n5.61\n\n\nWS17086\n2017-03-27\n31\n0\n1943\n0.0\n0.0\n0.0\nNA\n3.02\n5.22\n\n\nWS17170\n2017-06-19\n27\n0\n469\n0.0\n0.0\n0.0\nNA\n2.72\n4.46\n\n\nWS17212\n2017-07-31\n29\n3\n1816\n6.9\n10.3\n4.2\n2.5\n1.83\n3.53\n\n\nWS17282\n2017-10-09\n62\n0\n4656\n0.0\n0.0\n0.0\nNA\n2.77\n5.06\n\n\nWS18008\n2018-01-09\n33\n0\n1777\n0.0\n0.0\n0.0\nNA\n3.77\n5.93\n\n\nSV18067\n2018-03-08\n28\n0\n2584\n0.0\n0.0\n0.0\nNA\n3.03\n5.32\n\n\nWS18120\n2018-04-30\n19\n0\n893\n0.0\n0.0\n0.0\nNA\n2.69\n4.95\n\n\nSV18173\n2018-06-22\n29\n0\n1905\n0.0\n0.0\n0.0\nNA\n2.62\n5.19\n\n\nWS18218\n2018-08-06\n24\n0\n1762\n0.0\n0.0\n0.0\nNA\n2.18\n4.89\n\n\nWS18285\n2018-10-12\n61\n7\n4110\n0.0\n11.5\n1.7\n8.5\n1.11\n6.47\n\n\nWS18351\n2018-12-17\n12\n0\n1538\n0.0\n0.0\n0.0\nNA\n3.15\n4.81\n\n\nWS19028\n2019-01-28\n31\n0\n1290\n0.0\n0.0\n0.0\nNA\n3.66\n5.77\n\n\nWS19119\n2019-04-29\n43\n0\n2347\n0.0\n0.0\n0.0\nNA\n2.85\n5.06\n\n\nWS19210\n2019-07-29\n43\n0\n2437\n0.0\n0.0\n0.0\nNA\n2.50\n5.13\n\n\nWS19266\n2019-09-23\n64\n0\n5154\n0.0\n0.0\n0.0\nNA\n2.70\n5.32\n\n\nWS19322\n2019-11-18\n67\n0\n4182\n0.0\n0.0\n0.0\nNA\n3.29\n5.84\n\n\nWS20006\n2020-01-06\n54\n0\n4649\n0.0\n0.0\n0.0\nNA\n2.69\n5.23\n\n\nWS20231\n2020-08-18\n44\n0\n2968\n0.0\n0.0\n0.0\nNA\n2.51\n4.63\n\n\nWS20279\n2020-10-05\n63\n0\n4535\n0.0\n0.0\n0.0\nNA\n2.51\n4.94\n\n\nWS20342\n2020-12-07\n67\n0\n4341\n0.0\n0.0\n0.0\nNA\n2.74\n5.16\n\n\nWS21032\n2021-02-02\n64\n0\n4680\n0.0\n0.0\n0.0\nNA\n2.75\n5.49\n\n\nWS21093\n2021-04-03\n64\n0\n3989\n0.0\n0.0\n0.0\nNA\n2.98\n5.20\n\n\nWS21151\n2021-05-31\n73\n0\n5065\n0.0\n0.0\n0.0\nNA\n2.21\n5.18\n\n\nWS21212\n2021-07-31\n69\n1\n4194\n0.0\n1.4\n0.0\n16.3\n1.49\n4.57\n\n\nWS21278\n2021-10-05\n65\n2\n4419\n0.0\n3.1\n0.1\n10.4\n1.04\n5.92\n\n\nWS21338\n2021-12-04\n71\n0\n5673\n0.0\n0.0\n0.0\nNA\n2.50\n5.55\n\n\nWS22022\n2022-01-22\n61\n0\n4301\n0.0\n0.0\n0.0\nNA\n3.20\n5.14\n\n\nWS22072\n2022-03-14\n75\n0\n6295\n0.0\n0.0\n0.0\nNA\n2.65\n5.19\n\n\nWS22141\n2022-05-21\n64\n0\n4736\n0.0\n0.0\n0.0\nNA\n2.75\n4.92\n\n\nWB22215\n2022-08-03\n58\n2\n5074\n0.0\n3.4\n0.2\n5.5\n1.82\n5.13\n\n\nWS22281\n2022-10-08\n70\n0\n5271\n0.0\n0.0\n0.0\nNA\n2.75\n4.85\n\n\nWS22337\n2022-12-04\n77\n0\n5999\n0.0\n0.0\n0.0\nNA\n2.54\n6.06\n\n\nWS23011\n2023-01-11\n72\n0\n4414\n0.0\n0.0\n0.0\nNA\n3.21\n5.60\n\n\nWS23061\n2023-03-02\n84\n0\n6555\n0.0\n0.0\n0.0\nNA\n2.62\n5.10\n\n\nWS23203\n2023-07-22\n84\n2\n6756\n0.0\n2.4\n0.7\n17.1\n1.35\n5.98\n\n\nWS24139\n2024-05-18\n86\n0\n6869\n0.0\n0.0\n0.0\nNA\n3.17\n5.62\n\n\n\n\n\nGenerated on 2026-08-03 12:43 EDT."
  },
  {
    "objectID": "index.html",
    "href": "index.html",
    "title": "SFER MBON Oxygen",
    "section": "",
    "text": "Cruise reports generated from cleaned CTD data and DIVAnd-interpolated dissolved oxygen fields.\n\nOxygen field maps\nHypoxic depth maps\nHypoxic extent across cruises"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/SV18067.html",
    "href": "cruise_oxygen/batched_reports/SV18067.html",
    "title": "SV18067 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/SV18067.html#cruise-sv18067",
    "href": "cruise_oxygen/batched_reports/SV18067.html#cruise-sv18067",
    "title": "SV18067 Report",
    "section": "Cruise SV18067",
    "text": "Cruise SV18067\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    28             2624             42376            3.0303\n2    28             2624             42376            5.3231"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/SV18067.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/SV18067.html#interpolated-field-map",
    "title": "SV18067 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WB22215.html",
    "href": "cruise_oxygen/batched_reports/WB22215.html",
    "title": "WB22215 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WB22215.html#cruise-wb22215",
    "href": "cruise_oxygen/batched_reports/WB22215.html#cruise-wb22215",
    "title": "WB22215 Report",
    "section": "Cruise WB22215",
    "text": "Cruise WB22215\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    58             5093             27205            1.8209\n2    58             5093             27205            5.1271"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WB22215.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WB22215.html#interpolated-field-map",
    "title": "WB22215 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0612.html",
    "href": "cruise_oxygen/batched_reports/WS0612.html",
    "title": "WS0612 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0612.html#cruise-ws0612",
    "href": "cruise_oxygen/batched_reports/WS0612.html#cruise-ws0612",
    "title": "WS0612 Report",
    "section": "Cruise WS0612",
    "text": "Cruise WS0612\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    89            14423             11876            1.8646\n2    89            14423             11876            5.5601"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0612.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS0612.html#interpolated-field-map",
    "title": "WS0612 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0623.html",
    "href": "cruise_oxygen/batched_reports/WS0623.html",
    "title": "WS0623 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0623.html#cruise-ws0623",
    "href": "cruise_oxygen/batched_reports/WS0623.html#cruise-ws0623",
    "title": "WS0623 Report",
    "section": "Cruise WS0623",
    "text": "Cruise WS0623\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    33             1731             44718            1.4524\n2    33             1731             44718            3.7931"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0623.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS0623.html#interpolated-field-map",
    "title": "WS0623 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0718.html",
    "href": "cruise_oxygen/batched_reports/WS0718.html",
    "title": "WS0718 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0718.html#cruise-ws0718",
    "href": "cruise_oxygen/batched_reports/WS0718.html#cruise-ws0718",
    "title": "WS0718 Report",
    "section": "Cruise WS0718",
    "text": "Cruise WS0718\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    38             5167             17386            2.6713\n2    38             5167             17386            5.1821"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0718.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS0718.html#interpolated-field-map",
    "title": "WS0718 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0807.html",
    "href": "cruise_oxygen/batched_reports/WS0807.html",
    "title": "WS0807 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0807.html#cruise-ws0807",
    "href": "cruise_oxygen/batched_reports/WS0807.html#cruise-ws0807",
    "title": "WS0807 Report",
    "section": "Cruise WS0807",
    "text": "Cruise WS0807\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    37             5480             15685            2.4070\n2    37             5480             15685            5.1997"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0807.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS0807.html#interpolated-field-map",
    "title": "WS0807 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0906.html",
    "href": "cruise_oxygen/batched_reports/WS0906.html",
    "title": "WS0906 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0906.html#cruise-ws0906",
    "href": "cruise_oxygen/batched_reports/WS0906.html#cruise-ws0906",
    "title": "WS0906 Report",
    "section": "Cruise WS0906",
    "text": "Cruise WS0906\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    26             3737             28176            2.1732\n2    26             3737             28176            4.2541"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0906.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS0906.html#interpolated-field-map",
    "title": "WS0906 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0919.html",
    "href": "cruise_oxygen/batched_reports/WS0919.html",
    "title": "WS0919 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0919.html#cruise-ws0919",
    "href": "cruise_oxygen/batched_reports/WS0919.html#cruise-ws0919",
    "title": "WS0919 Report",
    "section": "Cruise WS0919",
    "text": "Cruise WS0919\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    25             3036             29357            2.6526\n2    25             3036             29357            4.5363"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS0919.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS0919.html#interpolated-field-map",
    "title": "WS0919 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1004.html",
    "href": "cruise_oxygen/batched_reports/WS1004.html",
    "title": "WS1004 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1004.html#cruise-ws1004",
    "href": "cruise_oxygen/batched_reports/WS1004.html#cruise-ws1004",
    "title": "WS1004 Report",
    "section": "Cruise WS1004",
    "text": "Cruise WS1004\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    31             3027             34386            2.7320\n2    31             3027             34386            5.6318"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1004.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS1004.html#interpolated-field-map",
    "title": "WS1004 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1015.html",
    "href": "cruise_oxygen/batched_reports/WS1015.html",
    "title": "WS1015 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1015.html#cruise-ws1015",
    "href": "cruise_oxygen/batched_reports/WS1015.html#cruise-ws1015",
    "title": "WS1015 Report",
    "section": "Cruise WS1015",
    "text": "Cruise WS1015\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    48             4751             18047            0.6045\n2    48             4751             18047            0.6065"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1015.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS1015.html#interpolated-field-map",
    "title": "WS1015 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1102.html",
    "href": "cruise_oxygen/batched_reports/WS1102.html",
    "title": "WS1102 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1102.html#cruise-ws1102",
    "href": "cruise_oxygen/batched_reports/WS1102.html#cruise-ws1102",
    "title": "WS1102 Report",
    "section": "Cruise WS1102",
    "text": "Cruise WS1102\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    33             5968             22640            1.7263\n2    33             5968             22640            3.5704"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1102.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS1102.html#interpolated-field-map",
    "title": "WS1102 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1113.html",
    "href": "cruise_oxygen/batched_reports/WS1113.html",
    "title": "WS1113 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1113.html#cruise-ws1113",
    "href": "cruise_oxygen/batched_reports/WS1113.html#cruise-ws1113",
    "title": "WS1113 Report",
    "section": "Cruise WS1113",
    "text": "Cruise WS1113\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    39            10058             41965            1.1699\n2    39            10058             41965            3.9663"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1113.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS1113.html#interpolated-field-map",
    "title": "WS1113 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1202.html",
    "href": "cruise_oxygen/batched_reports/WS1202.html",
    "title": "WS1202 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1202.html#cruise-ws1202",
    "href": "cruise_oxygen/batched_reports/WS1202.html#cruise-ws1202",
    "title": "WS1202 Report",
    "section": "Cruise WS1202",
    "text": "Cruise WS1202\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    33             4863             25041            1.7207\n2    33             4863             25041            4.8915"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1202.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS1202.html#interpolated-field-map",
    "title": "WS1202 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1212.html",
    "href": "cruise_oxygen/batched_reports/WS1212.html",
    "title": "WS1212 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1212.html#cruise-ws1212",
    "href": "cruise_oxygen/batched_reports/WS1212.html#cruise-ws1212",
    "title": "WS1212 Report",
    "section": "Cruise WS1212",
    "text": "Cruise WS1212\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    35             4956             22697            2.6969\n2    35             4956             22697            4.7057"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS1212.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS1212.html#interpolated-field-map",
    "title": "WS1212 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15103.html",
    "href": "cruise_oxygen/batched_reports/WS15103.html",
    "title": "WS15103 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15103.html#cruise-ws15103",
    "href": "cruise_oxygen/batched_reports/WS15103.html#cruise-ws15103",
    "title": "WS15103 Report",
    "section": "Cruise WS15103",
    "text": "Cruise WS15103\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    31             1721             45626            1.0065\n2    31             1721             45626            6.3063"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15103.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS15103.html#interpolated-field-map",
    "title": "WS15103 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15208.html",
    "href": "cruise_oxygen/batched_reports/WS15208.html",
    "title": "WS15208 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15208.html#cruise-ws15208",
    "href": "cruise_oxygen/batched_reports/WS15208.html#cruise-ws15208",
    "title": "WS15208 Report",
    "section": "Cruise WS15208",
    "text": "Cruise WS15208\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    45             7740             16079            1.4801\n2    45             7740             16079            5.5960"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15208.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS15208.html#interpolated-field-map",
    "title": "WS15208 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15320.html",
    "href": "cruise_oxygen/batched_reports/WS15320.html",
    "title": "WS15320 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15320.html#cruise-ws15320",
    "href": "cruise_oxygen/batched_reports/WS15320.html#cruise-ws15320",
    "title": "WS15320 Report",
    "section": "Cruise WS15320",
    "text": "Cruise WS15320\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    30             1411             44548            2.0856\n2    30             1411             44548            4.8811"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS15320.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS15320.html#interpolated-field-map",
    "title": "WS15320 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16074.html",
    "href": "cruise_oxygen/batched_reports/WS16074.html",
    "title": "WS16074 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16074.html#cruise-ws16074",
    "href": "cruise_oxygen/batched_reports/WS16074.html#cruise-ws16074",
    "title": "WS16074 Report",
    "section": "Cruise WS16074",
    "text": "Cruise WS16074\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    31             1152             72889            2.5237\n2    31             1152             72889            5.5489"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16074.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS16074.html#interpolated-field-map",
    "title": "WS16074 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16263.html",
    "href": "cruise_oxygen/batched_reports/WS16263.html",
    "title": "WS16263 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16263.html#cruise-ws16263",
    "href": "cruise_oxygen/batched_reports/WS16263.html#cruise-ws16263",
    "title": "WS16263 Report",
    "section": "Cruise WS16263",
    "text": "Cruise WS16263\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    28             1630             42251            2.0717\n2    28             1630             42251            4.9545"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS16263.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS16263.html#interpolated-field-map",
    "title": "WS16263 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17030.html",
    "href": "cruise_oxygen/batched_reports/WS17030.html",
    "title": "WS17030 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17030.html#cruise-ws17030",
    "href": "cruise_oxygen/batched_reports/WS17030.html#cruise-ws17030",
    "title": "WS17030 Report",
    "section": "Cruise WS17030",
    "text": "Cruise WS17030\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    29             1803             42730            2.8508\n2    29             1803             42730            5.6084"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17030.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS17030.html#interpolated-field-map",
    "title": "WS17030 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17170.html",
    "href": "cruise_oxygen/batched_reports/WS17170.html",
    "title": "WS17170 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17170.html#cruise-ws17170",
    "href": "cruise_oxygen/batched_reports/WS17170.html#cruise-ws17170",
    "title": "WS17170 Report",
    "section": "Cruise WS17170",
    "text": "Cruise WS17170\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    27              507             89909            2.7159\n2    27              507             89909            4.4615"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17170.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS17170.html#interpolated-field-map",
    "title": "WS17170 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17282.html",
    "href": "cruise_oxygen/batched_reports/WS17282.html",
    "title": "WS17282 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17282.html#cruise-ws17282",
    "href": "cruise_oxygen/batched_reports/WS17282.html#cruise-ws17282",
    "title": "WS17282 Report",
    "section": "Cruise WS17282",
    "text": "Cruise WS17282\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    63             4772             48832            2.7664\n2    63             4772             48832            5.0644"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS17282.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS17282.html#interpolated-field-map",
    "title": "WS17282 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18120.html",
    "href": "cruise_oxygen/batched_reports/WS18120.html",
    "title": "WS18120 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18120.html#cruise-ws18120",
    "href": "cruise_oxygen/batched_reports/WS18120.html#cruise-ws18120",
    "title": "WS18120 Report",
    "section": "Cruise WS18120",
    "text": "Cruise WS18120\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    20              924             60286            2.6945\n2    20              924             60286            4.9503"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18120.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS18120.html#interpolated-field-map",
    "title": "WS18120 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18285.html",
    "href": "cruise_oxygen/batched_reports/WS18285.html",
    "title": "WS18285 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18285.html#cruise-ws18285",
    "href": "cruise_oxygen/batched_reports/WS18285.html#cruise-ws18285",
    "title": "WS18285 Report",
    "section": "Cruise WS18285",
    "text": "Cruise WS18285\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    64             4193             24151            1.1090\n2    64             4193             24151            6.4699"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS18285.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS18285.html#interpolated-field-map",
    "title": "WS18285 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19028.html",
    "href": "cruise_oxygen/batched_reports/WS19028.html",
    "title": "WS19028 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19028.html#cruise-ws19028",
    "href": "cruise_oxygen/batched_reports/WS19028.html#cruise-ws19028",
    "title": "WS19028 Report",
    "section": "Cruise WS19028",
    "text": "Cruise WS19028\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    32             1329             62029            3.6554\n2    32             1329             62029            5.7670"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19028.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS19028.html#interpolated-field-map",
    "title": "WS19028 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19210.html",
    "href": "cruise_oxygen/batched_reports/WS19210.html",
    "title": "WS19210 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19210.html#cruise-ws19210",
    "href": "cruise_oxygen/batched_reports/WS19210.html#cruise-ws19210",
    "title": "WS19210 Report",
    "section": "Cruise WS19210",
    "text": "Cruise WS19210\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    43             2488             29445            2.5009\n2    43             2488             29445            5.1254"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19210.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS19210.html#interpolated-field-map",
    "title": "WS19210 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19322.html",
    "href": "cruise_oxygen/batched_reports/WS19322.html",
    "title": "WS19322 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19322.html#cruise-ws19322",
    "href": "cruise_oxygen/batched_reports/WS19322.html#cruise-ws19322",
    "title": "WS19322 Report",
    "section": "Cruise WS19322",
    "text": "Cruise WS19322\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    68             4326             24319            3.2941\n2    68             4326             24319            5.8361"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS19322.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS19322.html#interpolated-field-map",
    "title": "WS19322 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS20231.html",
    "href": "cruise_oxygen/batched_reports/WS20231.html",
    "title": "WS20231 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS20231.html#cruise-ws20231",
    "href": "cruise_oxygen/batched_reports/WS20231.html#cruise-ws20231",
    "title": "WS20231 Report",
    "section": "Cruise WS20231",
    "text": "Cruise WS20231\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    45             3034             30048            2.5097\n2    45             3034             30048            4.6257"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS20231.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS20231.html#interpolated-field-map",
    "title": "WS20231 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS20342.html",
    "href": "cruise_oxygen/batched_reports/WS20342.html",
    "title": "WS20342 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS20342.html#cruise-ws20342",
    "href": "cruise_oxygen/batched_reports/WS20342.html#cruise-ws20342",
    "title": "WS20342 Report",
    "section": "Cruise WS20342",
    "text": "Cruise WS20342\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    67             4489             24573            2.7412\n2    67             4489             24573            5.1600"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS20342.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS20342.html#interpolated-field-map",
    "title": "WS20342 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21093.html",
    "href": "cruise_oxygen/batched_reports/WS21093.html",
    "title": "WS21093 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21093.html#cruise-ws21093",
    "href": "cruise_oxygen/batched_reports/WS21093.html#cruise-ws21093",
    "title": "WS21093 Report",
    "section": "Cruise WS21093",
    "text": "Cruise WS21093\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    64             4075             52848            2.9809\n2    64             4075             52848            5.2020"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21093.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS21093.html#interpolated-field-map",
    "title": "WS21093 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21212.html",
    "href": "cruise_oxygen/batched_reports/WS21212.html",
    "title": "WS21212 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21212.html#cruise-ws21212",
    "href": "cruise_oxygen/batched_reports/WS21212.html#cruise-ws21212",
    "title": "WS21212 Report",
    "section": "Cruise WS21212",
    "text": "Cruise WS21212\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    69             4259             29494            1.4935\n2    69             4259             29494            4.5662"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21212.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS21212.html#interpolated-field-map",
    "title": "WS21212 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21338.html",
    "href": "cruise_oxygen/batched_reports/WS21338.html",
    "title": "WS21338 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21338.html#cruise-ws21338",
    "href": "cruise_oxygen/batched_reports/WS21338.html#cruise-ws21338",
    "title": "WS21338 Report",
    "section": "Cruise WS21338",
    "text": "Cruise WS21338\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    71             5697             25855            2.4974\n2    71             5697             25855            5.5520"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS21338.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS21338.html#interpolated-field-map",
    "title": "WS21338 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22072.html",
    "href": "cruise_oxygen/batched_reports/WS22072.html",
    "title": "WS22072 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22072.html#cruise-ws22072",
    "href": "cruise_oxygen/batched_reports/WS22072.html#cruise-ws22072",
    "title": "WS22072 Report",
    "section": "Cruise WS22072",
    "text": "Cruise WS22072\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    75             6458             23007            2.6545\n2    75             6458             23007            5.1897"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22072.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS22072.html#interpolated-field-map",
    "title": "WS22072 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22281.html",
    "href": "cruise_oxygen/batched_reports/WS22281.html",
    "title": "WS22281 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22281.html#cruise-ws22281",
    "href": "cruise_oxygen/batched_reports/WS22281.html#cruise-ws22281",
    "title": "WS22281 Report",
    "section": "Cruise WS22281",
    "text": "Cruise WS22281\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    72             5434             24968            2.7499\n2    72             5434             24968            4.8539"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS22281.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS22281.html#interpolated-field-map",
    "title": "WS22281 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS23011.html",
    "href": "cruise_oxygen/batched_reports/WS23011.html",
    "title": "WS23011 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS23011.html#cruise-ws23011",
    "href": "cruise_oxygen/batched_reports/WS23011.html#cruise-ws23011",
    "title": "WS23011 Report",
    "section": "Cruise WS23011",
    "text": "Cruise WS23011\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    72             4557             25992            3.2065\n2    72             4557             25992            5.6046"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS23011.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS23011.html#interpolated-field-map",
    "title": "WS23011 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS23203.html",
    "href": "cruise_oxygen/batched_reports/WS23203.html",
    "title": "WS23203 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/depth_layers.R\")))\nsource(here(glue(\"{params$batch_name}/plot_oxygen_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS23203.html#cruise-ws23203",
    "href": "cruise_oxygen/batched_reports/WS23203.html#cruise-ws23203",
    "title": "WS23203 Report",
    "section": "Cruise WS23203",
    "text": "Cruise WS23203\nThis report compares the DIVAnd-interpolated dissolved oxygen field with cleaned CTD observations from data/02_clean/.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    84             7037             23665            1.3486\n2    84             7037             23665            5.9850"
  },
  {
    "objectID": "cruise_oxygen/batched_reports/WS23203.html#interpolated-field-map",
    "href": "cruise_oxygen/batched_reports/WS23203.html#interpolated-field-map",
    "title": "WS23203 Report",
    "section": "Interpolated field map",
    "text": "Interpolated field map\nThe map below is interactive: pan and zoom to explore the gridded dissolved oxygen field. Use Prev / Next in the right-side panel to step through depth bands, and adjust the O₂ color scale limits in the same panel. CTD observations are colored with the same palette as the interpolated field.\n\n\nInteractive map with interpolated field and observation points\nif (is.null(field)) {\n  cat(\"Interpolated field not found. Run `make interpolate CRUISE=\", params$batch_value, \"` first.\", sep = \"\")\n} else {\n  map &lt;- plot_oxygen_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No field data available near the selected depth.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_oxygen/listing.html",
    "href": "cruise_oxygen/listing.html",
    "title": "cruise_oxygen",
    "section": "",
    "text": "Click on any element for more details.\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nSV18067 Report\n\n\n\n\n\n\n\n\n\n\n\n\nSV18173 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWB22215 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0603 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0612 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0618 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0623 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0704 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0718 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0802 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0807 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0901 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0906 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0914 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0919 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS0923 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1004 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1007 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1015 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1018 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1102 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1106 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1113 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1116 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1202 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1207 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS1212 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS14335 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS15103 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS15148 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS15208 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS15264 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS15320 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS16004 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS16074 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS16207 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS16263 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS16319 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS17030 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS17086 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS17170 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS17212 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS17282 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS18008 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS18120 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS18218 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS18285 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS18351 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS19028 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS19119 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS19210 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS19266 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS19322 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS20006 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS20231 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS20279 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS20342 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS21032 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS21093 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS21151 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS21212 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS21278 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS21338 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS22022 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS22072 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS22141 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS22281 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS22337 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS23011 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS23061 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS23203 Report\n\n\n\n\n\n\n\n\n\n\n\n\nWS24139 Report\n\n\n\n\n\nNo matching items"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/SV18173.html",
    "href": "cruise_hypoxic_depth/batched_reports/SV18173.html",
    "title": "SV18173 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/SV18173.html#cruise-sv18173",
    "href": "cruise_hypoxic_depth/batched_reports/SV18173.html#cruise-sv18173",
    "title": "SV18173 Report",
    "section": "Cruise SV18173",
    "text": "Cruise SV18173\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    29             1938             45866            2.6239\n2    29             1938             45866            5.1915"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/SV18173.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/SV18173.html#hypoxic-depth-map",
    "title": "SV18173 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0603.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS0603.html",
    "title": "WS0603 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0603.html#cruise-ws0603",
    "href": "cruise_hypoxic_depth/batched_reports/WS0603.html#cruise-ws0603",
    "title": "WS0603 Report",
    "section": "Cruise WS0603",
    "text": "Cruise WS0603\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    94            13066             12087            1.8824\n2    94            13066             12087            4.0451"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0603.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS0603.html#hypoxic-depth-map",
    "title": "WS0603 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0618.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS0618.html",
    "title": "WS0618 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0618.html#cruise-ws0618",
    "href": "cruise_hypoxic_depth/batched_reports/WS0618.html#cruise-ws0618",
    "title": "WS0618 Report",
    "section": "Cruise WS0618",
    "text": "Cruise WS0618\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    49             9319             12347            1.8812\n2    49             9319             12347            4.6943"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0618.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS0618.html#hypoxic-depth-map",
    "title": "WS0618 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0704.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS0704.html",
    "title": "WS0704 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0704.html#cruise-ws0704",
    "href": "cruise_hypoxic_depth/batched_reports/WS0704.html#cruise-ws0704",
    "title": "WS0704 Report",
    "section": "Cruise WS0704",
    "text": "Cruise WS0704\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    36             4950             18160            1.8837\n2    36             4950             18160            4.0938"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0704.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS0704.html#hypoxic-depth-map",
    "title": "WS0704 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0802.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS0802.html",
    "title": "WS0802 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0802.html#cruise-ws0802",
    "href": "cruise_hypoxic_depth/batched_reports/WS0802.html#cruise-ws0802",
    "title": "WS0802 Report",
    "section": "Cruise WS0802",
    "text": "Cruise WS0802\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    37             5394             17393            2.6808\n2    37             5394             17393            5.2205"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0802.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS0802.html#hypoxic-depth-map",
    "title": "WS0802 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0901.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS0901.html",
    "title": "WS0901 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0901.html#cruise-ws0901",
    "href": "cruise_hypoxic_depth/batched_reports/WS0901.html#cruise-ws0901",
    "title": "WS0901 Report",
    "section": "Cruise WS0901",
    "text": "Cruise WS0901\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    39             5131             18835            2.6348\n2    39             5131             18835            5.5332"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0901.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS0901.html#hypoxic-depth-map",
    "title": "WS0901 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0914.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS0914.html",
    "title": "WS0914 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0914.html#cruise-ws0914",
    "href": "cruise_hypoxic_depth/batched_reports/WS0914.html#cruise-ws0914",
    "title": "WS0914 Report",
    "section": "Cruise WS0914",
    "text": "Cruise WS0914\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    26             4671             24733            2.3144\n2    26             4671             24733            4.6715"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0914.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS0914.html#hypoxic-depth-map",
    "title": "WS0914 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0923.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS0923.html",
    "title": "WS0923 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0923.html#cruise-ws0923",
    "href": "cruise_hypoxic_depth/batched_reports/WS0923.html#cruise-ws0923",
    "title": "WS0923 Report",
    "section": "Cruise WS0923",
    "text": "Cruise WS0923\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    38             4544             31340            2.7817\n2    38             4544             31340            5.2614"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS0923.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS0923.html#hypoxic-depth-map",
    "title": "WS0923 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1007.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS1007.html",
    "title": "WS1007 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1007.html#cruise-ws1007",
    "href": "cruise_hypoxic_depth/batched_reports/WS1007.html#cruise-ws1007",
    "title": "WS1007 Report",
    "section": "Cruise WS1007",
    "text": "Cruise WS1007\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    35             4134             24248            2.7489\n2    35             4134             24248            5.5538"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1007.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS1007.html#hypoxic-depth-map",
    "title": "WS1007 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1018.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS1018.html",
    "title": "WS1018 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1018.html#cruise-ws1018",
    "href": "cruise_hypoxic_depth/batched_reports/WS1018.html#cruise-ws1018",
    "title": "WS1018 Report",
    "section": "Cruise WS1018",
    "text": "Cruise WS1018\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    32             4131             27485            1.1045\n2    32             4131             27485            2.0343"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1018.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS1018.html#hypoxic-depth-map",
    "title": "WS1018 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1106.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS1106.html",
    "title": "WS1106 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1106.html#cruise-ws1106",
    "href": "cruise_hypoxic_depth/batched_reports/WS1106.html#cruise-ws1106",
    "title": "WS1106 Report",
    "section": "Cruise WS1106",
    "text": "Cruise WS1106\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    30             4397             22864            2.1854\n2    30             4397             22864            4.2590"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1106.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS1106.html#hypoxic-depth-map",
    "title": "WS1106 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1116.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS1116.html",
    "title": "WS1116 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1116.html#cruise-ws1116",
    "href": "cruise_hypoxic_depth/batched_reports/WS1116.html#cruise-ws1116",
    "title": "WS1116 Report",
    "section": "Cruise WS1116",
    "text": "Cruise WS1116\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    24             5119             24591            2.7303\n2    24             5119             24591            4.6591"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1116.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS1116.html#hypoxic-depth-map",
    "title": "WS1116 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1207.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS1207.html",
    "title": "WS1207 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1207.html#cruise-ws1207",
    "href": "cruise_hypoxic_depth/batched_reports/WS1207.html#cruise-ws1207",
    "title": "WS1207 Report",
    "section": "Cruise WS1207",
    "text": "Cruise WS1207\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    36             4847             25511            2.1925\n2    36             4847             25511            4.1931"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS1207.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS1207.html#hypoxic-depth-map",
    "title": "WS1207 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS14335.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS14335.html",
    "title": "WS14335 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS14335.html#cruise-ws14335",
    "href": "cruise_hypoxic_depth/batched_reports/WS14335.html#cruise-ws14335",
    "title": "WS14335 Report",
    "section": "Cruise WS14335",
    "text": "Cruise WS14335\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    35              593             80084            3.4859\n2    35              593             80084            4.9883"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS14335.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS14335.html#hypoxic-depth-map",
    "title": "WS14335 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS15148.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS15148.html",
    "title": "WS15148 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS15148.html#cruise-ws15148",
    "href": "cruise_hypoxic_depth/batched_reports/WS15148.html#cruise-ws15148",
    "title": "WS15148 Report",
    "section": "Cruise WS15148",
    "text": "Cruise WS15148\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    32             1682             47579            1.9652\n2    32             1682             47579            4.8437"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS15148.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS15148.html#hypoxic-depth-map",
    "title": "WS15148 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS15264.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS15264.html",
    "title": "WS15264 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS15264.html#cruise-ws15264",
    "href": "cruise_hypoxic_depth/batched_reports/WS15264.html#cruise-ws15264",
    "title": "WS15264 Report",
    "section": "Cruise WS15264",
    "text": "Cruise WS15264\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    35             1894             32465            2.2688\n2    35             1894             32465            4.7455"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS15264.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS15264.html#hypoxic-depth-map",
    "title": "WS15264 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS16004.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS16004.html",
    "title": "WS16004 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS16004.html#cruise-ws16004",
    "href": "cruise_hypoxic_depth/batched_reports/WS16004.html#cruise-ws16004",
    "title": "WS16004 Report",
    "section": "Cruise WS16004",
    "text": "Cruise WS16004\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    32             1784             40849             2.709\n2    32             1784             40849             4.459"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS16004.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS16004.html#hypoxic-depth-map",
    "title": "WS16004 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS16207.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS16207.html",
    "title": "WS16207 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS16207.html#cruise-ws16207",
    "href": "cruise_hypoxic_depth/batched_reports/WS16207.html#cruise-ws16207",
    "title": "WS16207 Report",
    "section": "Cruise WS16207",
    "text": "Cruise WS16207\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    30             1680             43754            2.6401\n2    30             1680             43754            5.1674"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS16207.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS16207.html#hypoxic-depth-map",
    "title": "WS16207 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS16319.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS16319.html",
    "title": "WS16319 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS16319.html#cruise-ws16319",
    "href": "cruise_hypoxic_depth/batched_reports/WS16319.html#cruise-ws16319",
    "title": "WS16319 Report",
    "section": "Cruise WS16319",
    "text": "Cruise WS16319\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    36             1754             45295            3.0559\n2    36             1754             45295            5.0266"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS16319.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS16319.html#hypoxic-depth-map",
    "title": "WS16319 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS17086.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS17086.html",
    "title": "WS17086 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS17086.html#cruise-ws17086",
    "href": "cruise_hypoxic_depth/batched_reports/WS17086.html#cruise-ws17086",
    "title": "WS17086 Report",
    "section": "Cruise WS17086",
    "text": "Cruise WS17086\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    32             2015             42740            3.0186\n2    32             2015             42740            5.2248"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS17086.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS17086.html#hypoxic-depth-map",
    "title": "WS17086 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS17212.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS17212.html",
    "title": "WS17212 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS17212.html#cruise-ws17212",
    "href": "cruise_hypoxic_depth/batched_reports/WS17212.html#cruise-ws17212",
    "title": "WS17212 Report",
    "section": "Cruise WS17212",
    "text": "Cruise WS17212\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    29             1879             43531            1.8337\n2    29             1879             43531            3.5262"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS17212.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS17212.html#hypoxic-depth-map",
    "title": "WS17212 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS18008.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS18008.html",
    "title": "WS18008 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS18008.html#cruise-ws18008",
    "href": "cruise_hypoxic_depth/batched_reports/WS18008.html#cruise-ws18008",
    "title": "WS18008 Report",
    "section": "Cruise WS18008",
    "text": "Cruise WS18008\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    33             1842             36649            3.7695\n2    33             1842             36649            5.9260"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS18008.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS18008.html#hypoxic-depth-map",
    "title": "WS18008 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS18218.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS18218.html",
    "title": "WS18218 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS18218.html#cruise-ws18218",
    "href": "cruise_hypoxic_depth/batched_reports/WS18218.html#cruise-ws18218",
    "title": "WS18218 Report",
    "section": "Cruise WS18218",
    "text": "Cruise WS18218\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    25             1789             45670            2.1798\n2    25             1789             45670            4.8945"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS18218.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS18218.html#hypoxic-depth-map",
    "title": "WS18218 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS18351.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS18351.html",
    "title": "WS18351 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS18351.html#cruise-ws18351",
    "href": "cruise_hypoxic_depth/batched_reports/WS18351.html#cruise-ws18351",
    "title": "WS18351 Report",
    "section": "Cruise WS18351",
    "text": "Cruise WS18351\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    12             1556             54512            3.1524\n2    12             1556             54512            4.8139"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS18351.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS18351.html#hypoxic-depth-map",
    "title": "WS18351 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS19119.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS19119.html",
    "title": "WS19119 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS19119.html#cruise-ws19119",
    "href": "cruise_hypoxic_depth/batched_reports/WS19119.html#cruise-ws19119",
    "title": "WS19119 Report",
    "section": "Cruise WS19119",
    "text": "Cruise WS19119\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    44             2402             29505            2.8493\n2    44             2402             29505            5.0629"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS19119.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS19119.html#hypoxic-depth-map",
    "title": "WS19119 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS19266.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS19266.html",
    "title": "WS19266 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS19266.html#cruise-ws19266",
    "href": "cruise_hypoxic_depth/batched_reports/WS19266.html#cruise-ws19266",
    "title": "WS19266 Report",
    "section": "Cruise WS19266",
    "text": "Cruise WS19266\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    64             5288             22003            2.7045\n2    64             5288             22003            5.3170"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS19266.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS19266.html#hypoxic-depth-map",
    "title": "WS19266 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS20006.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS20006.html",
    "title": "WS20006 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS20006.html#cruise-ws20006",
    "href": "cruise_hypoxic_depth/batched_reports/WS20006.html#cruise-ws20006",
    "title": "WS20006 Report",
    "section": "Cruise WS20006",
    "text": "Cruise WS20006\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    54             4737             23604            2.6919\n2    54             4737             23604            5.2266"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS20006.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS20006.html#hypoxic-depth-map",
    "title": "WS20006 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS20279.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS20279.html",
    "title": "WS20279 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS20279.html#cruise-ws20279",
    "href": "cruise_hypoxic_depth/batched_reports/WS20279.html#cruise-ws20279",
    "title": "WS20279 Report",
    "section": "Cruise WS20279",
    "text": "Cruise WS20279\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    64             4677             23902            2.5146\n2    64             4677             23902            4.9411"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS20279.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS20279.html#hypoxic-depth-map",
    "title": "WS20279 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS21032.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS21032.html",
    "title": "WS21032 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS21032.html#cruise-ws21032",
    "href": "cruise_hypoxic_depth/batched_reports/WS21032.html#cruise-ws21032",
    "title": "WS21032 Report",
    "section": "Cruise WS21032",
    "text": "Cruise WS21032\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    68             4777             22432            2.7498\n2    68             4777             22432            5.4938"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS21032.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS21032.html#hypoxic-depth-map",
    "title": "WS21032 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS21151.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS21151.html",
    "title": "WS21151 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS21151.html#cruise-ws21151",
    "href": "cruise_hypoxic_depth/batched_reports/WS21151.html#cruise-ws21151",
    "title": "WS21151 Report",
    "section": "Cruise WS21151",
    "text": "Cruise WS21151\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    74             5166             25207            2.2067\n2    74             5166             25207            5.1807"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS21151.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS21151.html#hypoxic-depth-map",
    "title": "WS21151 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS21278.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS21278.html",
    "title": "WS21278 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS21278.html#cruise-ws21278",
    "href": "cruise_hypoxic_depth/batched_reports/WS21278.html#cruise-ws21278",
    "title": "WS21278 Report",
    "section": "Cruise WS21278",
    "text": "Cruise WS21278\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    66             4463             23368            1.0401\n2    66             4463             23368            5.9163"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS21278.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS21278.html#hypoxic-depth-map",
    "title": "WS21278 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS22022.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS22022.html",
    "title": "WS22022 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS22022.html#cruise-ws22022",
    "href": "cruise_hypoxic_depth/batched_reports/WS22022.html#cruise-ws22022",
    "title": "WS22022 Report",
    "section": "Cruise WS22022",
    "text": "Cruise WS22022\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    61             4415             25523            3.1962\n2    61             4415             25523            5.1376"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS22022.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS22022.html#hypoxic-depth-map",
    "title": "WS22022 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS22141.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS22141.html",
    "title": "WS22141 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS22141.html#cruise-ws22141",
    "href": "cruise_hypoxic_depth/batched_reports/WS22141.html#cruise-ws22141",
    "title": "WS22141 Report",
    "section": "Cruise WS22141",
    "text": "Cruise WS22141\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    65             4834             25124            2.7484\n2    65             4834             25124            4.9152"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS22141.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS22141.html#hypoxic-depth-map",
    "title": "WS22141 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS22337.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS22337.html",
    "title": "WS22337 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS22337.html#cruise-ws22337",
    "href": "cruise_hypoxic_depth/batched_reports/WS22337.html#cruise-ws22337",
    "title": "WS22337 Report",
    "section": "Cruise WS22337",
    "text": "Cruise WS22337\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    79             6166             24499            2.5375\n2    79             6166             24499            6.0611"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS22337.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS22337.html#hypoxic-depth-map",
    "title": "WS22337 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS23061.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS23061.html",
    "title": "WS23061 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS23061.html#cruise-ws23061",
    "href": "cruise_hypoxic_depth/batched_reports/WS23061.html#cruise-ws23061",
    "title": "WS23061 Report",
    "section": "Cruise WS23061",
    "text": "Cruise WS23061\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    86             6802             22404            2.6248\n2    86             6802             22404            5.1036"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS23061.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS23061.html#hypoxic-depth-map",
    "title": "WS23061 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS24139.html",
    "href": "cruise_hypoxic_depth/batched_reports/WS24139.html",
    "title": "WS24139 Report",
    "section": "",
    "text": "Load cleaned observations and interpolated oxygen field\nsource(here(glue(\"{params$batch_name}/getData.R\")))\nsource(here(\"R/color_scales.R\"))\nsource(here(glue(\"{params$batch_name}/hypoxic_depth.R\")))\nsource(here(glue(\"{params$batch_name}/plot_hypoxic_depth_map.R\")))\n\ndata &lt;- getData(params$batch_value)\nobs &lt;- data$observations\nfield &lt;- data$oxygen_field"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS24139.html#cruise-ws24139",
    "href": "cruise_hypoxic_depth/batched_reports/WS24139.html#cruise-ws24139",
    "title": "WS24139 Report",
    "section": "Cruise WS24139",
    "text": "Cruise WS24139\nThis report maps the shallowest depth at which the interpolated dissolved oxygen field falls below a chosen threshold (hypoxic boundary). Use the slider on the map to change the O₂ threshold; grid cells are colored by depth in meters.\n\n\nObservation and field summary\ndata.frame(\n  casts = length(unique(obs$cast_id)),\n  observation_rows = nrow(obs),\n  field_grid_points = if (is.null(field)) NA_integer_ else nrow(field),\n  oxygen_range_mg_l = if (\"dissolved_oxygen\" %in% names(obs)) {\n    range(obs$dissolved_oxygen, na.rm = TRUE)\n  } else {\n    c(NA_real_, NA_real_)\n  }\n)\n\n\n  casts observation_rows field_grid_points oxygen_range_mg_l\n1    87             7118             22680            3.1716\n2    87             7118             22680            5.6171"
  },
  {
    "objectID": "cruise_hypoxic_depth/batched_reports/WS24139.html#hypoxic-depth-map",
    "href": "cruise_hypoxic_depth/batched_reports/WS24139.html#hypoxic-depth-map",
    "title": "WS24139 Report",
    "section": "Hypoxic depth map",
    "text": "Hypoxic depth map\nPan and zoom to explore where oxygen drops below the threshold. Each colored cell shows the shallowest depth (closest to the surface) where interpolated O₂ is below the threshold — not the depth where O₂ equals the threshold. When the threshold exceeds surface oxygen across the domain, expect uniform shallow depths (~2 m, the top of the interpolated grid). Hover a cell for its depth in meters. Use the depth color scale inputs to adjust the color bar limits.\n\n\nInteractive map of shallowest hypoxic depth\nif (is.null(field)) {\n  cat(\n    \"Interpolated field not found. Run `make interpolate CRUISE=\",\n    params$batch_value,\n    \"` first.\",\n    sep = \"\"\n  )\n} else {\n  map &lt;- plot_hypoxic_depth_map(field, obs)\n  if (is.null(map)) {\n    cat(\"No interpolated field grid available for this cruise.\")\n  } else {\n    map\n  }\n}"
  }
]